# 心流模式（Flow Mode）技术设计

> 创建：2026-09-23 ｜ 状态：**已实施（P1–P3 全部交付，已随 v0.8.0 发布）** ｜ 需求见 [flow-mode.md](flow-mode.md) ｜ 实施记录见第 10 节
> 分支：`feature/flow-mode`

## 1. 总体方案

**一句话**：新增两条互相独立的渲染层能力——「打字机锚定」是一个 CodeMirror 6 扩展（ViewPlugin + 留白 theme），「心流模式」是 app store 上的一个布尔预设（进入时快照外围状态、拨动各轴，退出时还原）。不引入任何新依赖，不改动磁盘产物。

```
app store（渲染态）
 ├─ editorWysiwyg       （已有，轴 1：源码 / 所见即所得）
 ├─ zenMode             （已有，轴 2：常规 / 专注）
 ├─ typewriterMode      （新，轴 3：off | center | bottom ← settings 持久化）
 └─ flowMode: boolean   （新；预设 = 一组开关的组合 + 外围状态快照）

MarkdownEditor.vue
 ├─ livePreview Compartment        （已有）
 └─ typewriter Compartment         （新：lib/typewriter.ts）
      ├─ 留白 theme（.cm-content 上下 padding = 锚点比例 × 视口高）
      └─ updateListener → rAF 合并 → 坐标差值滚动

lib/caretSound.ts                  （新：Web Audio 回车音效合成器，无资源文件）
EditorView.vue                     （心流按钮 / 栏宽变量 / 保存指示挂载）
```

三条轴与预设的关系见需求文档 1.3；本设计只负责把「打字机」做成一条干净的轴、把「心流」做成纯组合。

## 2. 决策记录

| # | 决策点 | 结论 | 理由与备选 |
| --- | --- | --- | --- |
| D1 | 打字机实现层 | **自研 CM6 扩展**（ViewPlugin + theme），不依赖 CSS `scroll-snap` 或第三方包 | 需要按「事件类型」决定是否重锚（输入锚、滚动不锚、组词冻结），纯 CSS 无法表达；CM6 是现有编辑层，扩展成本可控（一个模块）。备选 `scroll-padding` 方案无法实现「用户滚动不干预」 |
| D2 | 锚点可达性 | **给 `.cm-content` 加动态上下留白**：`paddingTop = ratio × 视口高`、`paddingBottom = (1 − ratio) × 视口高` | 文档首行/末行也要能到达锚点线；留白量随视口尺寸变化（ResizeObserver → CSS 变量），高位 = 50%/50%（同 Typora），低位 = 80%/20% |
| D3 | 滚动定位算法 | **屏幕坐标差值法**，不用 `EditorView.scrollIntoView` | `scrollIntoView` 只支持 start/center/end/nearest，表达不了「80% 处」；且其坐标基准与 padding 的关系有歧义。差值法直接自洽：`scrollTop += coordsAtPos(head).top − (scrollerTop + height × ratio)` |
| D4 | 重锚触发规则 | 见第 3 节规则表：输入 / 撤销 / 键盘移动 / 点击定位 → 锚；**用户滚动、拖选中、IME 组词中 → 不锚** | 需求 D5；拖选连续锚定会眩晕，组词锚定会让候选框抖（所见即所得已有 `view.composing` 冻结的成熟先例） |
| D5 | 扩展挂载方式 | 沿用既有 **Compartment** 模式（同 `livePreview`）：切换打字机形态只重配扩展，不重建 EditorView | 重建 EditorView 会丢失滚动位置与撤销历史；Compartment 是项目已验证的做法 |
| D6 | 心流预设语义 | `flowMode` 是 app store 运行态；进入时把外围状态存入 `flowSnapshot`，退出还原；**心流偏好（打字机形态 / 栏宽 / 音效）存 settings，不参与快照** | 需求 FR-F2 的状态语义表；复用 `zenMode.previewBeforeZen` 已有的快照先例，不发明第二套机制 |
| D7 | 音效合成参数 | 短噪声脉冲（带通 ~1.8kHz）+ 低频正弦（~140Hz）双分量，指数衰减，逐次随机微调；限流 120ms | 需求 D6；双分量是「thock」质感的关键（高频给"咔"、低频给"木"），单噪声听起来像爆音 |
| D8 | 音效触发点 | 在 CM6 `updateListener` 里检测 `tr.isUserEvent('input')` 且 **changes 插入内容含 `\n`** | 比监听 keydown 准确：IME 确认键、粘贴多行、列表自动续行都能正确区分（粘贴多行不发声，回车发声） |
| D9 | 保存指示位置 | 编辑卡右下角内侧的 6px 圆点，`position: absolute`，仅心流模式渲染 | 不占布局、不打断输入；数据源复用 editor store 既有 `dirty` / `saving` / `externalChanged`，不新增状态 |
| D10 | 栏宽实现 | `.cm-content { max-width: var(--editor-measure); margin-inline: auto }`，心流模式下注入变量 | 与打字机的上下 padding 天然共存（方向不同）；三档映射 32em / 42em / 52em（中文约 1em/字） |

## 3. 打字机滚动规则表（实现契约）

| 事件 | 是否重锚 | 说明 |
| --- | --- | --- |
| 输入字符 / 回车 / 删除 | ✅ | 主场景；`tr.isUserEvent('input' | 'delete')` |
| 撤销 / 重做 | ✅ | `undo` / `redo` |
| 键盘移动光标（方向键 / Home / End / Ctrl+Home） | ✅ | Typora 同款手感 |
| 鼠标点击定位 | ✅ | `select.pointer` 且非拖选 |
| 鼠标拖选过程中 | ❌ | mousedown → 挂起，mouseup → 补一次锚定 |
| 用户滚动（滚轮 / 滚动条 / 触控板） | ❌ | **绝不干预**；不产生事务，天然不触发；下次输入回到锚点 |
| IME 组词期间 | ❌ | `view.composing === true` 时直接跳过；`compositionend` 后补一次 |
| 程序化写入（外部文件变更、切换笔记、模式切换） | ❌ | 切换笔记由既有逻辑归零滚动，避免两套滚动互相打架 |

**合并策略**：所有重锚请求进 `requestAnimationFrame` 队列，一帧内多次变更只滚动一次（连续快速输入不掉帧）。

**与预览联动**：重锚修改的是 `scrollDOM.scrollTop`，会触发既有的编辑器 `scroll` 监听 → 自动同步预览（现有 100ms 防回环守卫只拦截「预览→编辑器」的回流，不冲突）。无需新增同步代码。

## 4. 模块与文件布局

```
src/shared/types.ts                    # + typewriterMode / flowLineWidth / flowSoundEnabled / flowSoundVolume
src/main/index.ts                      # 三处默认值同步（JsonStore 默认）
src/renderer/src/
├─ lib/typewriter.ts                   # CM6 扩展：typewriterExtension(ratio) / 锚点滚动 / 留白管理
├─ lib/caretSound.ts                   # Web Audio 音效合成器（init / playReturn / setVolume / dispose）
├─ components/MarkdownEditor.vue       # 挂载 typewriter Compartment；updateListener 里触发音效
├─ views/EditorView.vue                # 心流按钮 / Esc 退出 / 栏宽变量 / 保存指示
├─ stores/app.ts                       # flowMode / flowSnapshot / enterFlow / exitFlow（+ DEFAULT_SETTINGS）
└─ styles/main.css                     # .flow-mode 相关（栏宽变量、保存指示样式）
src/renderer/src/config/shortcuts.ts   # 登记 Alt+W；Esc 描述更新
tests/caretSound.test.ts 等            # 纯函数单测（见第 8 节）
```

## 5. 关键实现要点

### 5.1 锚点计算（纯函数，可单测）

```ts
/** 目标滚动位置：把光标的屏幕坐标对齐到编辑区 ratio 处（ratio 0.5 = 高位居中；0.8 = 低位） */
export function anchorScrollTop(
  cursorTop: number,      // view.coordsAtPos(head).top（屏幕坐标）
  scrollerTop: number,    // view.scrollDOM.getBoundingClientRect().top
  viewportHeight: number, // scrollDOM.clientHeight
  currentScrollTop: number,
  ratio: number
): number {
  return currentScrollTop + (cursorTop - (scrollerTop + viewportHeight * ratio))
}
```

### 5.2 留白管理

- 由扩展在挂载时读取 `scrollDOM.clientHeight`，写入 CSS 变量：`--tw-pad-top = ratio × H`、`--tw-pad-bottom = (1 − ratio) × H`；
- theme 规则：`.cm-content { padding-top: var(--tw-pad-top); padding-bottom: var(--tw-pad-bottom) }`；
- `ResizeObserver` 监听 scrollDOM 尺寸变化（窗口缩放、栏宽调整、预览分栏拖拽）后重算并补一次锚定；
- **关闭打字机时清零两个变量**（验收标准 10：无残留 padding）。

### 5.3 音效合成（`lib/caretSound.ts`）

```
playReturn():
  1. 限流：now - lastPlayAt < 120ms → return
  2. AudioContext 懒创建（首次播放；打字即用户手势，满足自动播放策略）
  3. 分量 A（"咔"）：白噪声 12ms buffer → Bandpass(freq≈1800Hz±6%, Q≈0.8) → Gain 包络（8ms 起音 / 45ms 指数衰减）
  4. 分量 B（"木"）：Sine(≈140Hz±6%) → Gain 包络（指数衰减 80ms）
  5. 主增益 = volume/100 × 0.5（上限防爆音）
  6. 播放后释放节点；dispose() 时 close AudioContext
```

无任何音频文件：零安装包体积、零版权问题，音色参数可后续调（未来若要更精致，可再评估随包 CC0 素材）。

### 5.4 心流预设（`app.ts`）

```ts
enterFlow():   // 快照外围状态 → 拨动各轴
  flowSnapshot = { sidebarVisible, zenMode, previewVisible, editorWysiwyg }
  zenMode = true; sidebarVisible = false; previewVisible = false; editorWysiwyg = true
  // 打字机 / 栏宽 / 保存指示 / 音效：由 flowMode=true 驱动（各组件自行响应 settings）

exitFlow():    // 还原外围状态；心流偏好不动
  还原 flowSnapshot → flowMode = false
```

- `Esc` 分级回退链插入位置：`关闭悬浮预览 → **退出心流** → 返回上级文件夹 → 关闭网格`（仅编辑视图且有内容时）；
- 心流模式内 Alt+B / Ctrl+E 等仍可用（用户手动调整仅影响本次会话，退出时按快照还原）。

### 5.5 设置项清单（需三处同步：types / 主进程默认值 / 渲染端 DEFAULT_SETTINGS + 测试夹具）

| 设置键 | 类型 | 默认 | 归属 |
| --- | --- | --- | --- |
| `typewriterMode` | `'off' \| 'center' \| 'bottom'` | `'off'` | 轴 3（独立于心流） |
| `flowLineWidth` | `'narrow' \| 'medium' \| 'wide'` | `'medium'` | 心流偏好 |
| `flowSoundEnabled` | `boolean` | `false` | 心流偏好 |
| `flowSoundVolume` | `number`（0–100） | `60` | 心流偏好 |
| `flowSoundVariant` | `'wood' \| 'metal' \| 'ratchet' \| 'rotate'` | `'wood'` | 心流偏好（2026-09-24 按试听反馈新增） |

## 6. 实施首日需实测确认的点

1. `.cm-content` 的上下 padding 是否影响 `coordsAtPos` 基准（差值法理论上不依赖，但需实测首行/末行锚定是否准确）；
2. `.cm-content` 设 `max-width` + `margin-inline: auto` 后，换行位置、光标绘制、选区内边距、行级滚动同步（`data-source-line` 坐标）是否全部正常；
3. 低位锚点在不同窗口高度下的观感（默认 0.8 是否合适，必要时微调到 0.75）；
4. 所见即所得模式下打字机与装饰刷新（Widget 重渲染）叠加时的滚动稳定性。

> 四项均已在 P1 / P2 实测闭环：① 留白改加在 `.cm-content`（滚动层 padding 会撑破几何，见 10.1）；② 栏宽方案由 `max-width + margin-inline: auto` 改为滚动层两侧内边距（自动外边距会让宽度塌陷，见 10.2 决策 1）；③ 0.8 观感合适，未调整；④ 与装饰刷新叠加无抖动。

## 7. 分阶段实施计划

| 阶段 | 范围 | 依赖 | 可独立发版 | 状态 |
| --- | --- | --- | --- | --- |
| **P1 打字机** | FR-F1（含高/低位、规则表全部行为）+ 设置项 + 单测 | 无 | ✅ | ✅ 2026-09-23（见 10.1） |
| **P2 心流预设** | FR-F2 / F3 / F5 + Alt+W + Esc + 栏宽 + 保存指示 | P1 | ✅ | ✅ 2026-09-23（见 10.2） |
| **P3 音效** | FR-F4（合成器 + 设置项 + 限流） | P2（作用范围） | ✅ | ✅ 2026-09-23（音色扩展 09-24，见 10.3） |
| P4 可选（未排期） | 环境音、写作统计、光标动效 | — | — | 未开始 |

每期完成后按仓库规范同步：CHANGELOG `[未发布]`、PRD 补记 FR 编号（打字机 → FR-2.4.14；心流 → FR-2.9.8；音效 → FR-2.9.9）、`requirements/index.md` 状态、用户手册。

## 8. 测试计划

**单元测试（vitest）**：

- `anchorScrollTop`：高位/低位、首行/末行、滚动位置无关性；
- 留白量计算：ratio 与视口高度 → padding 映射；
- 音效限流：`shouldPlay(now, lastPlayAt)` 边界（=119ms 不响 / =120ms 响）；
- 栏宽档位 → em 映射。

**手动验证清单（对应需求第 5 节验收标准）**：

- 中文输入法组词全程无抖动；组词确认后锚定正确；
- 拖选不连续滚动、长文（≥5000 行）连续输入无卡顿；
- 用户滚动查看前文不被弹回，输入后回到锚点；
- 心流进入/退出状态往返（含「心流内 Alt+B 唤出侧栏 → 退出后状态正确」）；
- 音效关闭后无残留音频上下文（`AudioContext.state === 'closed'`）；
- 关闭打字机后对比常规模式滚动行为完全一致。

## 9. 风险与对策

| 风险 | 对策 |
| --- | --- |
| padding 与坐标基准不符导致锚点偏移 | 差异法（D3）对基准不敏感；首日实测项 1 兜底 |
| 与所见即所得装饰刷新叠加产生滚动抖动 | 重锚走 rAF 合并；实测项 4 专项验证 |
| 心流内用户手动调整与退出还原产生「我的操作被撤销了」的困惑 | 快照语义写入用户手册 + 设置页说明；如真实反馈困扰，再评估改为「退出保留用户手调」 |
| 音效实现后音色不满意（合成器上限） | 参数可调（频率/衰减/滤波）；不满意时的退路是随包 CC0 素材（不改架构，只换 `playReturn` 实现） |
| 打字机模式下长按方向键连续滚动掉帧 | rAF 合并；必要时对「连续方向键」加 16ms 节流（实测决定） |

---

## 10. 实施记录

### 10.1 P1 打字机模式（2026-09-23 完成）

**交付**：`src/renderer/src/lib/typewriter.ts`（CM6 扩展）+ 设置项 `typewriterMode`（types / 主进程默认值 / 渲染端默认值 / 测试夹具 五处同步）+ 设置 → 编辑器 UI + `tests/typewriter.test.ts`（12 项）。

**实测发现（重要，实现修正了设计初稿的一处判断）**：

- **留白必须加在内容层（`.cm-content`），不能加在滚动层（`.cm-scroller`）**：`.cm-scroller` 的高度由内容撑开，给它加 padding 会把自身撑得超过父容器（实测 765px 容器被撑到 1764px），滚动几何错乱，且插件读取它的 `clientHeight` 会形成反馈放大（padding 越大 → clientHeight 越大 → padding 更大）。已确认正确做法：padding 加在 `.cm-content`，滚动层盒子保持稳定，行号 gutter 由 CM6 按内容坐标定位、自动跟随（实测「第 1 行」文本与行号 `1` 的 y 坐标均为 471，严格对齐）。
- **设计自洽性得到验证**：scrollTop = 0 时（刚打开笔记、未发生任何滚动），首行恰好落在锚点线上——因为 `paddingTop = ratio × 视口高`。切换笔记无需额外锚定动作，观感即正确。
- **锚点公式（差值法）实测精确**：高位模式下光标屏幕位置恒为 383（= 0.5 × 765），低位模式恒为 612（= 0.8 × 765）；连续输入时 `scrollTop` 递增而光标 Y 不变，即需求的「文字上移、光标不动」。
- **既有 `.cm-scroller` 的 `padding: 12px 0 40vh` 保留**：它是滚过末尾的既有福利，与打字机留白叠加只是多出一些可滚动空间，无副作用；关闭打字机时清零的是 `.cm-content` 的内联 padding，滚动行为与常规完全一致（实测 `padding: (none)`、光标到文末为常规滚动）。

**已验证验收项**（真实键事件驱动，非合成事件）：

| 验收标准 | 结果 |
| --- | --- |
| 高位 / 低位在源码模式生效，光标锚定精确 | ✅ 383 / 612（= 比例 × 视口高） |
| 所见即所得模式下同样生效且与装饰渲染无干扰 | ✅ Ctrl+E 切换后锚定不变，输入时文字流动 |
| 输入时「文字上移、光标不动」 | ✅ scrollTop 2057→2082→2108，光标 Y 恒 383 |
| 用户滚动查看前文不被弹回；下次输入回到锚点 | ✅ 上滚 300 后未被弹回，再输入回到 383 |
| 方向键移动光标触发重锚 | ✅ 上键后回到 383 |
| 拖选过程中不连续滚动，拖选结束补锚定 | ✅ 拖选中 scrollTop 不变，mouseup 后回锚点 |
| 关闭模式后留白清零、行为与常规一致 | ✅ `padding: (none)`，Ctrl+End 为常规滚动 |
| 窗口尺寸变化后留白重算 | ✅ 视口 765→598 后 padding 383→299 |

**待人工确认项**：中文输入法组词的实机表现（代码路径已按 `view.composing` 冻结 + `compositionend` 补锚实现，但 CDP 无法驱动真实 IME；建议在 P2 开发时一并实测）。

### 10.2 P2 心流预设（2026-09-23 完成）

**交付**：`lib/flow.ts`（栏宽映射 + 打字机形态推导，纯函数）+ 设置项 `flowLineWidth`（五处同步）+ app store 的 `flowMode` / `flowSnapshot` / `enterFlow` / `exitFlow` + `Alt+W` 快捷键与顶栏咖啡杯按钮 + 写作栏宽 CSS + 保存指示 + 设置页「心流模式」分组 + `tests/flow.test.ts`（6 项）。

**实现中的三处关键决策 / 修正**：

| # | 问题 | 结论 |
| --- | --- | --- |
| 1 | 栏宽用「滚动层 max-width + 自动外边距」时宽度塌陷 | 自动外边距会取代 flex 的 stretch，`max-width: 630px` 被算成 304px（内容宽）。最终改为**在滚动层加两侧内边距**（`padding-inline: max(0, (100% − 栏宽) / 2)`）——内容列同样居中，且滚动条仍留在卡片右缘（max-width 方案会让滚动条悬在卡片中间），窗口更窄时自动收敛为 0 |
| 2 | 打字机形态是否算「心流偏好」 | 需求原表把「打字机形态」列在心流偏好（退出不改、下次沿用）。实现简化为**推导式**：心流内 `off → bottom`，用户选过则沿用自己的形态；不改写设置项，故无需快照还原。好处是四条轴（侧栏 / 专注 / 预览 / 打字机）语义完全一致，也少一个冗余设置项（需求文档的状态语义表已同步为此语义） |
| 3 | 顶栏隐藏与侧栏浮层的归属 | 顶栏隐藏不写设置项（不改 `zenHideTopbar`），由 `concealed = flowMode \|\| (zenMode && zenHideTopbar)` 推导；侧栏浮层（Alt+B 呼出）在沉浸态复用既有的 `zenSidebarOverlay`，不引入第二套浮层状态 |

**顺带修复的三个既有缺陷**（P2 实测暴露，均与本次改动同源：多所有者 / 时序）：

- **Ctrl+F 会弹出编辑器原生查找面板**：`MarkdownEditor` 里「禁用原生搜索」的绑定写的是 `run: () => false`——CM 语义中 false 表示「未处理」，会继续落到 basicSetup 的 `searchKeymap` 上，查找面板照常弹出并抢走焦点。改为 `Prec.high(keymap.of([...]))` 提升优先级 + `return true` 消费按键（CM 只 preventDefault 不阻断冒泡，全局搜索仍会打开）。
- **Esc 分级回退会一次退两级**：浮层侧栏、悬浮预览、心流退出原本分散在 App 与 EditorView 两个 window 监听器里，同一次按键会因执行顺序互相看到对方刚改过的状态（实测：关浮层的同时把心流也退了）。现统一为 App.vue 单一入口，并改用**捕获阶段**判断——必须早于 Element Plus 对话框自身的 Esc 处理，否则等冒泡到窗口时对话框已关闭，「有模态则让位」的判断失效。
- **退出所见即所得后预览分栏状态丢失**：该联动原在 EditorView 的 watcher 中，组件重挂载会丢掉「进入前分栏状态」的记忆（模块级变量），且与心流的进入 / 退出互相覆盖（实测退出心流后预览没能还原）。现移入 app store 的 `setEditorWysiwyg` 统一维护。

**已验证验收项**（CDP 真实键事件）：

| 验收标准 | 结果 |
| --- | --- |
| `Alt+W` 进入：侧栏 / 迷你导航条 / 顶栏隐藏、切所见即所得、打字机生效、栏宽生效、保存指示出现 | ✅ |
| 打字机在心流内默认低位（锚点 = 视口高 × 0.8） | ✅ 678 / 678 |
| 写作栏宽三档：窄 470 / 中 395 / 宽 320（两侧内边距，随窗口宽反算一致） | ✅ |
| 保存指示：输入 → dirty、保存中 → saving、成功 → saved、静置 → 淡出 | ✅ dirty / saved / （无） |
| `Esc` 退出并还原进入前状态（预览可见 + 源码模式） | ✅ |
| 「专注态下进心流 → 退出仍回专注态」 | ✅ |
| `Alt+B` 在心流内呼出浮层侧栏；再按 Esc 只关浮层、心流保持 | ✅ |
| 心流内 `Ctrl+F` 打开全局搜索（无 CM 查找面板） | ✅ |
| `Ctrl+,` 在心流内打开设置；Esc 先回编辑（仍在心流），再 Esc 才退出心流 | ✅ |

### 10.3 P3 回车音效（2026-09-23 完成）

**交付**：`lib/caretSound.ts`（Web Audio 合成器：限流与合成参数为纯函数，可单测）+ 设置项 `flowSoundEnabled` / `flowSoundVolume` / `flowSoundVariant`（五处同步）+ 设置页开关、音色选择与音量滑杆 + `tests/caretSound.test.ts`（当前 12 项：限流与合成参数 6 项、音色参数 6 项）。

**实现要点**：

- 触发点：CM6 事务判定为「输入类且插入内容含 `\n`」时播放；显式排除 `input.paste`（粘贴多行不发声）。比监听 keydown 准确——IME 确认键、粘贴、列表自动续行都能正确区分。
- 音效作用范围：父组件仅在「心流模式且开关开启」时置 `enabled`，组件内不判断模式（单一职责）。
- 音频上下文懒创建（首次播放时才建，打字本身即用户手势，满足自动播放策略）；关闭开关即 `dispose()`（`ctx.close()`）。
- 音色参数集中在 `synthParams()`，将来若要更精致的音色，替换该函数即可（架构不变）。
  > 2026-09-24 后续迭代已改写音色实现：木质等三色下线，调参入口改为 `retroParams()` / `RETRO_BELL_PARTIALS` / `returnRatchetSchedule()`（见 10.6–10.8）。

**已验证验收项**（CDP 包装 `AudioContext` 计数探测，代替「听」）：

| 验收标准 | 结果 |
| --- | --- |
| 非心流模式回车不发声 | ✅ 振荡器/噪声源/上下文创建数均为 0 |
| 心流内回车 1 次 = 1 组合成音 | ✅ osc=1 noise=1 ctx=1 |
| 连续回车限流 | ✅ 间隔 40ms 连按 5 次仅放行 2 次 |
| 粘贴多行不发声 | ✅ 增量 +0 |
| 关闭开关后不发声且释放上下文 | ✅ 增量 +0，`AudioContext.state === 'closed'` |

**音色扩展（2026-09-24，按用户反馈）**：初版木质音被认为「有些沉闷」，遂增加三种音色并做成可选项（设置项 `flowSoundVariant`）：

| 音色 | 合成方式 | 音色特征 |
| --- | --- | --- |
| `wood` 木质 | 噪声脉冲 12ms + 带通 ~2kHz（咔）+ 正弦 140Hz 衰减 80ms（木） | 薄膜键盘闷响（按反馈把带通由 1.8kHz 提亮到 2kHz、木质分量略降） |
| `metal` 金属 | FM：载波 ~1.5kHz + 调制比 2.76、调制指数由 1.8 快速收敛 → 敲击泛音；90ms 延迟 + 0.28 反馈 + 2.6kHz 低通做回声 | 清脆「叮」带回声，尾巴 ~0.27s 衰减殆尽 |
| `ratchet` 打字机 | 预生成 0.8s 缓冲：一串木质咔哒（间隔 14ms→9ms 渐密、±12% 抖动、频率 700–1600Hz、中间略强的包络），每齿为 6ms 噪声 + 正弦共振、1.5ms 衰减 | 指甲划过木齿 / 老式打字机拉回车的「唰」 |
| `rotate` 轮换 | 每次回车在木/金/棘之间依次轮换 | 长时间写作不重复 |

同种音色再次触发时先停掉上一声（避免叠加成噪音），异种音色可共存；棘齿缓冲按采样率缓存，不重复生成。

**排障记录（教训）**：首轮验证「木质与棘齿不发声」是**测试探针自身的 bug**——探针的子类覆盖写成 `createBuffer() { return super.createBuffer() }`（漏传参数），使这两条路径在 `createBuffer` 处抛错；金属路径不用 buffer 故「正常」。写探针时必须透传参数（`(...a)`）。产品代码本身无误。

**音色试听反馈（2026-09-24，首轮）**：

- **打字机（棘齿）**——最接近预期，仍需微调；
- **木质**——偏「沉闷」，待改进；
- **金属**——待改进（回声与音色本身）。

用户计划后续用音频分析工具提供频谱 / 时域数据，据此继续调参，故音色部分标记为**打磨中**（功能已完成、参数待迭代）。调参入口同上。

### 10.5 音色扩展（二）：按实录音效复刻「复古打字机」（2026-09-24 晚）

**背景**：用户提供实录音效（爱给网「复古打字机 Sound_FX_2」，1.695s / 44.1kHz 立体声）作为音色参考，并说明本机不方便做频谱分析。按项目既有决策（D6：程序合成、不随包音频文件，避免版权与体积），做法是**离线分析其声学特征 → 参数化重建**，不引入任何音频素材。

**分析口径（可复现）**：`ffprobe` 看格式 → `ffmpeg` 转单声道 48kHz PCM → Node 脚本做 ① 2ms 帧 RMS 包络做击打检测 ② 每次击打的攻击 / 衰减（-20dB）③ 2048 点 FFT 的频段占比与质心 ④ RBJ biquad 逐频段 4ms 包络（拟合起振先后）。

**目标实录音效的实测结论**：

| 项 | 实测值 |
| --- | --- |
| 结构 | 1.695s 内 7 次击打（136 / 508 / 534 / 712 / 794 / 914 / 1328 ms）——含连续按键，以及 534ms 处一个 **0.50s 衰减的回车铃**（~9.3–10.7kHz，质心 8008Hz） |
| 单次按键（@1328ms，最干净的一击） | 攻击 **0.4ms**、-20dB 衰减 **24ms**、峰值 0.498 |
| 频段占比（0–42ms 窗） | 低<250Hz 13.9% ｜ 中低 250–800Hz 13.6% ｜ 中 800–2kHz 15.7% ｜ **中高 2–5kHz 29.0%** ｜ 高 5–12kHz 21.7% ｜ 质心 3942Hz |
| 共振峰 | 低频簇 94 / 117 / 129 / 176 / 211Hz（机体）+ 387 / 504Hz（纸卷） + 2742Hz（字锤）+ 6527Hz |
| 逐频段包络 | 高频起振即达峰、~12ms 衰到 10%（咔）；低频 160–400Hz 起振即强、40–60ms 余振；**40–160Hz 在 ~20ms 才到峰**（机体「咚」晚于咔） |

**重建方式**（`lib/caretSound.ts` 的 `renderRetroStrike`；后续迭代已更名为 `renderRetroReturn()` 并拆出 `renderRetroPush()` / `renderRetroBell()`，见 10.6–10.8；三段叠加 + 峰值归一化）：

| 成分 | 参数 | 对应实测 |
| --- | --- | --- |
| ① 高频「咔」 | 噪声经一阶高通（100Hz 以下 −20dB）+ 一阶低通整形，指数衰减 4.8ms | 高频起振即达峰、~12ms 衰减 |
| ② 字锤 / 纸卷敲击 | 正弦 320Hz（30ms）+ 520Hz（18ms）+ 1200Hz（20ms）+ 2900Hz（16ms），各带 ±6% 抖动 | 387/504/2742Hz 共振与 800Hz–5kHz 的能量峰 |
| ③ 机体「咚」 | 正弦 96Hz + 182Hz，**延迟 12ms** 起振、90ms 余振、4ms 起音斜坡 | 40–160Hz 在 ~20ms 达峰、余振 40–60ms |
| 抖动 | 频率 ±6~7%、衰减 ±15~18%，另加播放速率 ±3% | 连续回车不机械重复 |

**客观比对（同一套指标，合成 vs 目标 @1328ms）**：

| 指标 | 目标 | 合成 |
| --- | --- | --- |
| 频段占比 | 13.9 / 13.6 / 15.7 / 29.0 / 21.7 % | 13.9 / 14.3 / 20.4 / 24.0 / 12.2 % |
| 质心 | 3942Hz | 4908Hz |
| 主要峰 | 2742 / 117 / 164 / 375Hz | 2953 / 1219 / 94 / 328Hz |
| 包络（高频） | 99→41→21→11（4ms/格） | 99→49→30→22 |
| 包络（低频） | 99→95→75→92→70 | 99→83→65→49→49 |

残余差异：高频段占比仍偏低（12% vs 22%），听感上会比录音稍「闷」一点——如需更亮可调噪声项系数（现为 2.9）与低通系数（0.72，均在 `renderRetroReturn()` 内）；进一步精调建议以实听为准。

**未随包音频**：项目仍不引入任何音频文件（版权 + 体积，D6 不变）；本次先重建了「按键」这一声，**回车铃于同日补入（见 10.6）**。

**方法论沉淀**：以后用户再给音色参考，按同样口径分析（ffprobe/ffmpeg → 包络 + FFT 频段占比 + biquad 逐频段包络）即可照此复刻；分析脚本为一次性临时脚本（未入库），需要时可整理成 `scripts/analyze-audio.mjs` 长期保留。

### 10.6 音色扩展（二）迭代：补上「推回车」与「回车铃」（2026-09-24 深夜）

**用户反馈**（首轮试听）：① 希望**带上回车铃**——「能给人一种愉悦感」；② 现在这声太短，「人类能听到的只有一个比较短的音」——原素材其实是采集的打字机**推回车**那种棘轮音，复刻丢了这层机械动作感。

**补充分析（同一套口径）**：

| 发现 | 实测 |
| --- | --- |
| 即使「单次按键」也不是单脉冲 | 孤立段（100–220ms）在 2ms 帧包络上有 **9 个能量峰**（116/124/142/148/154/164/174/180/188ms）——键帽 / 连杆 / 字锤 / 纸卷相继撞击 |
| 「推回车」= 密集棘轮 | 连续段尾部 **916–1000ms** 出现**间隔 6–10ms** 的密集能量峰（约 14–16 颗），即用户说的「往推」；前半段（576–834ms）则是间隔 30–60ms 的离散按键 |
| 回车铃的分音与衰减 | **2742 / 6527 / 9700 / 11449Hz** 四个分音，衰减 0.42 / 0.34 / 0.28 / 0.22s；铃响期间棘轮仍在推进，所以铃的包络呈起伏（不是干净的正弦衰减）；铃的峰值约为按键的 50% |

**新合成结构**（`renderRetroReturn()`，整段 **0.66s**，仍为单缓冲、零振荡器）：

| 时间 | 成分 | 对应实测 |
| --- | --- | --- |
| 0ms | 按键「咔」：噪声脉冲 + **四个微冲击**（7/14/23ms，振幅 0.42/0.30/0.20）+ 字锤 / 纸卷共振（320 / 520 / 1.2k / 2.9kHz）+ 延迟 12ms 的机体「咚」（96 / 182Hz） | 9 个能量峰的微冲击串；低频 20ms 处二次隆起 |
| 70–200ms | **推回车棘轮**：`returnRatchetSchedule()` 约 18 颗齿、间隔 9ms 渐密到 6ms、振幅 0.5→1.0 渐强（噪声 2.5ms + 木质共振 1.5ms 包络） | 916–1000ms 的 6–10ms 密集齿 |
| 200ms 起 | **回车铃**：`RETRO_BELL_PARTIALS` 四音（2742 / 6527 / 9700 / 11449Hz，振幅 0.5 / 0.3 / 0.24 / 0.13，衰减 0.42 / 0.34 / 0.28 / 0.22s），1.5ms 起音斜坡 | 铃的分音与各自衰减 |

**合成结果的实测结构**（2ms 帧包络，相对整段峰值）：按键段 0–40ms 有 3–4 个能量峰 → 推回车段 70–206ms **18 颗齿**可见、振幅由 12 升到 53 → 铃段 210ms 起平滑衰减 53→16（0.66s 收尾），铃分音各自按 τ 衰减 ✓。

**听感定位**：铃的音高（2.7k / 9.7kHz 分音）在有高音单元的扬声器上很清脆；若在小尺寸笔记本扬声器上嫌铃太尖或太弱，调 `RETRO_BELL_PARTIALS` 的振幅比即可（例如把 2742Hz 抬到 0.7、9700Hz 降到 0.15 会更「木挂牌铃」而不是「金属小铃」）。

**仍可继续打磨**：木质 / 金属两个老音色（见 10.4），以及铃在**每一次回车**都响是否嫌多——目前按用户要求每次都响；若要更克制可选「每 N 次回车响一次」。

### 10.7 音色扩展（二）再迭代：加长 + 连续换行屏蔽（2026-09-24 深夜，二轮反馈）

**用户反馈**（二轮试听）：① 「棘轮的声音**加长一倍**」；② 「铃声频率差不多，最后的**余音再加长 50%、音量提高 15%**」；③ 「每次回车都响铃还是给用户一个选择——做一个设置项**连续换行屏蔽音效**：快速连续换行时只有第一次回车有声音」。

**参数调整**：

| 项 | 改动 |
| --- | --- |
| 推回车棘轮 | 时长 0.13s → **0.26s**（齿数 18 → 约 35，间隔仍由 9ms 渐密到 6ms、振幅渐强） |
| 回车铃余振 | 各分音衰减 **×1.5**（0.63 / 0.51 / 0.42 / 0.33s），分音振幅 **×1.15**（0.575 / 0.345 / 0.276 / 0.15），铃起始 0.2s → 0.24s |
| 整段时长 | 0.66s → **1.0s**，并在末尾加 **60ms 淡出**（原先余振被缓冲直接截断） |

**实现中发现并修掉的问题（重要，勿再踩）**：按整段峰值归一化时，**棘轮的噪声尖峰会把按键「咔」压小**——棘轮有 30 余颗齿，任一齿撞上噪声床的大幅值就会成为全局峰值（实测 4 个噪声种子里有 3 个的全局峰值落在 240–300ms 的棘轮/铃上，导致按键听起来比棘轮轻）。现改为**三层分别渲染 + 按键定标**：棘轮、铃各自限制在按键峰值 0.6 / 0.8 以内；再按「两层叠加后的峰值」（240–330ms 两者同时在响）整体压低到按键的 0.85；最后按按键峰值归一化——这样「按键始终是这一声里最强的那一下」是**构造保证**，不再取决于噪声运气。

**新设置项 `flowSoundSkipRepeat`（设置 → 通用 → 心流模式 →「连续换行屏蔽音效」，默认开启）**：

- 语义：与上一次「回车插入」间隔 **< 800ms**（`CONSECUTIVE_RETURN_WINDOW_MS`）即视为同一串快速换行，**只有这串的第一次发声**；`lastReturnAt` 每次都会推进，所以一串 N 次换行只响一次（而不是每 800ms 响一次）。
- 判定收敛在纯函数 `gateReturn(now, state, skipConsecutive)`（可单测）：限流窗口 120ms 始终生效（防机关枪），屏蔽窗口只在设置开启时叠加。
- 实测（真实键盘注入 + 统计缓冲创建）：屏蔽开启时 641ms / 464ms 的连按均静默、停 1s 后恢复发声；屏蔽关闭时 450ms 间隔的两次换行都发声。

**实测结构（合成结果）**：整段 1.00s；按键微冲击 4 / 24 / 34ms；推回车 70–330ms 内 35 颗齿（34 个可见能量峰）；铃段 240–950ms 的包络平滑衰减（相对峰值 53 → 13）；末尾样本值 0.0000（淡出干净）。

**听感定位与可调项**：铃的余音现在约 0.7s（比原先长 50%）；若嫌整段偏长，可把 `RETRO_BELL_PARTIALS` 的 τ 收回 1.2 倍；若嫌铃尖，调低 9700 / 11449Hz 的振幅占比。`CONSECUTIVE_RETURN_WINDOW_MS` 现为 800ms，可按手感调整。

### 10.8 音色收敛：拆出「推回车 / 回车铃」两个独立音色，下线三种旧音色（2026-09-24 深夜，三轮反馈）

**用户反馈**（三轮试听，全部满意）：配比与屏蔽窗口都合适；要求 ① 把**棘轮**与**铃音**拆成两个**独立可选**的音色；② **下线**旧的木质 / 金属 / 打字机（棘齿）三个选项；③ **保留轮换**，并注意「旧的选择被去掉」会不会有引用问题。

**改动**：

| 项 | 改动 |
| --- | --- |
| 音色枚举 | `wood / metal / ratchet / retro / rotate` → **`retro / carriage / bell / rotate`**（4 项） |
| 新增独立音色 | `carriage` 推回车（棘轮）——0.3s，即 retro 里的棘轮段单独成声（起点对齐 0、末尾 30ms 淡出、峰值归一化）；`bell` 回车铃——0.8s，即 retro 里的铃单独成声（末尾 60ms 淡出） |
| 删除 | `wood` / `metal` / `ratchet` 的合成实现与相应单测（连同 `synthParams` / `clickComponent` / `bodyComponent` / `metalComponent` / `ratchetSchedule` / `ratchetComponent` / `RATCHET_DURATION_S`），播放器收敛为「波形缓存 + 通用缓冲播放」一条路径 |
| 默认音色 | `'wood'` → **`'retro'`**（音效默认仍为关闭，只在用户开启后才谈音色） |
| 轮换 | 变为 **推回车棘轮 → 回车铃 → 复古打字机 → 循环**（`CONCRETE_VARIANTS` 收敛为三项） |

**旧值引用问题的处理（用户特别提醒的一点）**：

1. **持久化旧值迁移**：`main/index.ts` 在启动时校验 `flowSoundVariant`，命中 `SOUND_VARIANTS`（`carriage / bell / retro / rotate`）之外的值（即旧安装里的 `wood` / `metal` / `ratchet`）→ 一次性改写为 `'retro'`。实测：把配置文件改回 `wood` 后启动，读到的设置与设置页均显示 `retro` ✓（否则会出现选择器空白 + 静默无声）。
2. **播放侧兜底**：`playReturn` 的 `switch` 用 `default` 承接 `retro`（以及任何漏网的过期值），保证「未知音色也一定发声」。
3. **类型收敛**：`AppSettings['flowSoundVariant']` 与 `SoundVariant` 同步收窄为四个值。顺带修掉一处此前埋下的类型缺陷——`AppSettings.flowSoundVariant` 的联合类型早先漏了 `'retro'`（设置流是弱类型的，运行时一直没暴露），现已修正。
4. **未做迁移提示**：旧音色从未随版本发布（整个迭代都在未发版区间内），故不需要面向用户的迁移说明，只在代码与设计文档留痕。

**实测**：

| 场景 | 结果 |
| --- | --- |
| 旧值迁移 | 配置文件写入 `wood` → 启动后为 `retro` ✓ |
| 三种音色波形 | `carriage` 0.30s / `bell` 0.80s / `retro` 1.00s，峰值均归一化到 0.95 ✓ |
| 四个选项均能发声 | 逐个真实播放（含 `rotate`）各创建 1 个缓冲源 ✓ |
| 设置页选项 | 渲染为「复古打字机（按键 + 推回车 + 回车铃）/ 推回车（棘轮）/ 回车铃 / 轮换（三者依次交替）」，旧三项已消失 ✓ |
| 单测 | `tests/caretSound.test.ts` 20 项（移除旧音色用例、新增两个独立音色的波形与淡出断言）全绿 |

### 10.4 遗留事项（截至 2026-09-24）

**待打磨**（用户实测反馈，均已登记在 [../index.md](../index.md) 第二节）：

- ~~回车音色参数迭代~~（**已完成**，见 10.5–10.8）：实录音效已复刻为四种音色（复古打字机 / 推回车棘轮 / 回车铃 / 轮换），旧的木质 / 金属 / 打字机三色随之下线；当前无待改的音色实现。若将来再提供参考音频，按 10.5 的分析口径（ffprobe → 帧包络 → 击打检测 → FFT 频段占比 → biquad 逐频段包络）复刻，调参入口为 `lib/caretSound.ts` 的 `retroParams()` / `RETRO_BELL_PARTIALS` / `returnRatchetSchedule()`。
- **心流栏宽下英文连续字母的行尾折行观感**：行尾输入连续字母（如 `kkk`）时，前两个字母先停在行尾、第三个到达后整串一起换行；拉丁字母串被当作一个「词」（与多数编辑器一致），如需逐字母断行可评估 `overflow-wrap: anywhere`。

**待验证**：

- **打字机 + 中文输入法组词的实机表现**：代码路径已按 `view.composing` 冻结 + `compositionend` 补锚实现，但 CDP 无法驱动真实 IME，需在真机（Windows 微软拼音 / Linux fcitx）确认候选框不抖动、组词确认后锚定正确（对应需求第 5 节验收标准第 3 项）。

**未排期**：

- P4 可选项：环境音、写作统计 HUD、光标视觉动效（需求第 4 节「非目标」）。
