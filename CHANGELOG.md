# 更新日志

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 新功能

- **常用 / 收藏卡片网格**：点击侧栏「常用」或「收藏」标题，主区域以卡片网格展示笔记（标题 + 内容摘要 + 所属库 / 路径）；**单击**卡片自右侧呼出悬浮预览，**双击**直接进入编辑；再次点击标题或 × 关闭。网格打开时编辑 / 预览卡自动让位，且不影响用户保存的分栏偏好
- **卡片操作菜单**：网格卡片悬浮出现 ⋮ 菜单——常用区提供 收藏 / 移出常用（仅移出列表记录，不删除笔记文件）/ 删除笔记；收藏区提供 取消收藏 / 删除笔记；删除为红色警示，确认后移入回收站
- **取消收藏**：除收藏区卡片菜单外，笔记树菜单按状态显示「收藏笔记 / 取消收藏」（此前收藏后无法取消）

### 修复

- 修复「移出常用」点击后列表不刷新的问题（`recent:remove` IPC 未返回结果对象，渲染端读取时抛错）
- 修复点击「移出常用」后视口左上角闪现菜单残影的问题：下拉菜单的 popper 锚点行在收起动画完成前被列表刷新移除所致。卡片菜单在执行移除类操作前等待收起动画结束；侧栏改为单行入口后此类锚点行已不存在
- 修复进入专注模式后预览卡片不隐藏的问题：进入专注时自动收起预览并记住进入前状态（退出后恢复）；专注模式下用预览按钮呼出的预览与编辑区各占 50%（与需求一致），且持久化的预览偏好不再被专注切换覆盖

### 变更

- **侧栏「常用 / 收藏」改为单行入口**（参考语雀设计）：点击标题直接打开主区域卡片网格，行内列表与展开箭头移除，数量以角标显示；收藏过多时不再拉长侧栏，也从结构上消除了下拉菜单锚点失效导致的闪影
- **回收站改为卡片风格列表**：条目以与常用 / 收藏网格一致的圆角卡片呈现（图标 + 名称 + 类型 + 原位置 + 删除时间 + 操作），保留列表形态，「网格 / 列表切换」为规划功能
- 网格悬浮预览支持**点击空白区域关闭**（此前仅 × / Esc）
- 侧栏区块标题精简：去掉「笔记库」前的文件夹图标与各区块占位块，每个标题仅保留一个前导元素（图标或展开箭头），标题文字列对齐
- 侧栏展开按钮美化（保留于「笔记库」区）：填充三角替换为细线箭头，旋转过渡更平滑，箭头热区带悬停底色反馈
- 右键 / 操作菜单顺序统一：普通操作在上，「删除」（红色警示）永远放最后并以分隔线隔开
- 新增 `recent:remove` IPC 通道（移出常用）

## [0.2.0] - 2026-09-06

### 界面重构

- **三栏圆角卡片布局**：侧栏、编辑区、预览区成为灰色画布上的三张独立圆角卡片，间距统一 10px；欢迎 / 回收站 / 设置页同步采用卡片风格
- 面包屑导航与格式工具栏并入编辑卡片，移除原生菜单栏（撤销 / 重做内嵌工具栏）
- 分栏拖拽间隙优化：分隔条改为透明间隙，悬停显示圆角细条，全部分栏间距统一为 10px

### 新功能

- **预览收缩**：编辑卡顶栏一键隐藏 / 显示预览卡片，状态跨启动记忆
- **专注模式**：四角方框按钮隐藏侧栏只留编辑区；专注下仍可呼出预览（编辑 / 预览各占一半）
- **悬浮预览**：长按预览按钮（约 0.5 秒）呼出悬浮预览卡片，覆盖编辑区不改变分栏布局；`Esc` / × / 再点按钮关闭
- **附件目录可配置**：设置 → 通用中自定义粘贴图片的保存目录，支持多级路径（如 `media/image`）

### 修复

- 修复分栏间隙不一致（分隔条被两侧间距叠加，30px → 统一 10px）
- 修复专注模式下预览按钮无响应的问题
- 修复侧栏笔记高亮在切换到回收站 / 设置页后不消失的问题
- 修复中文系统下新建仓库后关联失败的问题（仓库已创建但应用误报失败）
- 修复侧栏父子层级缩进错位
- 修复中文 locale 下读取未初始化仓库的 git 状态时产生无谓错误日志

### 变更

- 界面用语「子目录」统一改为「文件夹」
- 删除类菜单项（删除笔记库 / 文件夹 / 笔记）改为红色警示
- 回收站入口简化：点击「回收站」标题直达列表，去掉二级「查看回收站」
- 图片引用路径改用内容 hash 做外部修改检测，替代毫秒精度不可靠的时间戳

## [0.1.0] - 2026-09-05

首个公开版本。

### 核心

- 笔记库 → 文件夹（最多 6 层）→ 笔记的树形管理，创建 / 重命名 / 删除全程重名校验与非法字符过滤
- Markdown 编辑（CodeMirror 6）+ 实时预览：LaTeX 公式（KaTeX）、代码高亮、格式工具栏
- 自动保存（1s 防抖）与外部修改保护
- 图片粘贴 / 拖入，存入库内附件目录，相对路径引用
- 收藏与常用（最近打开）列表
- 应用级回收站：还原 / 彻底删除 / 清空

### 云同步

- GitHub Personal Access Token 登录（系统加密存储，含新手引导）
- 笔记库关联已有 / 新建远程仓库，一键同步（提交 → 拉取 → 推送）
- 冲突检测：列出冲突文件并保护本地内容

### 其他

- 浅色 / 深色 / 跟随系统主题
- 插件骨架（manifest + 加载器 + 管理页）与示例插件
- Windows（NSIS）/ macOS（DMG）/ Linux（deb + AppImage）安装包
