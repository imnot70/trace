# 更新日志

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [0.8.8] - 2026-09-26

### 新功能

- **草稿笔记（速记本）**（FR-2.3.9，用户提案，需求与设计见 [2026-09-26_draft-notes/](ai/requirements/2026-09-26_draft-notes/draft-notes.md)）：`Ctrl+N` 新建草稿——真实文件写入应用数据目录（`userData/scratch/`），不进笔记库 / 双链与反向链接 / 标签 / Git 同步体系（独立空间语义）；自动命名「速记 MMDD-HHmm」（重名追加序号），当前为空草稿时复用不新建，创建后自动聚焦编辑器。侧栏新增「草稿」菜单（草稿存在时显示）：列出全部草稿（名 + 相对时间），点击打开、⋮ 转正 / 永久删除（红色确认，不进回收站），创建后菜单即时刷新。草稿与常规笔记同样享受 1s 防抖自动保存（任何时刻崩溃都有磁盘副本），贴图 / 拖入图片存草稿自有附件目录（`scratch/assets/`）。`Ctrl+S` / 保存按钮 / 侧栏 ⋮ 进入**转正**：选目标库与目录（树形选择）并命名后移入笔记库参与 Git 同步，图片附件随迁并把引用改写为目标库附件目录路径（`assets/…` → `attachments/…`），编辑器无缝切换到正式路径。草稿可进入全局搜索：结果徽标显示「草稿」、范围下拉作为独立可勾选项（「所有库」语义包含草稿），自动保存增量更新索引、转正 / 删除时同步移除。主实现：`ScratchService` + `promoteDraft` 编排 + `DraftPromoteDialog`，新增单测 9 项（合计 364 项全绿）。
- **跨库插入引用校验**（FR-2.9.10 补充，技术评审发现）：双链只在库内解析，在库 X 的笔记里对搜索预览的库 Y 笔记点「插入引用 / Alt+Enter」会产出**永远断链**的 `[[引用]]` 且无提示（v0.8.6 起存在）。现插入前校验目标库与当前库，不一致则阻止并明确提示；`[[` 补全不受影响（补全源本就只来自当前库）；草稿内引用放行（草稿是暂存，转正时才确定归宿，D7 决议）。
- **写作底色纹理：羊皮纸 / 书页纤维**（用户提出）：底色新增「底色纹理」选择——纯色 / 羊皮纸 / 书页纤维。纯 CSS 程序化纹理：羊皮纸为双层 SVG feTurbulence 暖棕颗粒（细颗粒无缝拼接），书页纤维为极细横竖纹模拟纸张纤维方向；纹理叠加在所选底色上，与底色过渡共存，无图片素材、零体积。（随 v0.8.8 发布——该功能在 v0.8.7 打标签后才合入 main，v0.8.7 安装包不含此项。）

### 网站

- **新增官方静态站点与 GitHub Pages 自动部署**：`site/` 单页官网（项目介绍、特性、截图、下载入口、指引链接，纸感笔记风、浅色主题、与应用一致的设计语言）+ `.github/workflows/pages.yml` 自动部署（`site/**` 推送或手动触发；首次使用需在仓库 Settings → Pages 将 Source 切换为 GitHub Actions；workflow 已带 `enablement: true` 自动启用参数。站点已上线：`https://imnot70.github.io/trace/`。

## [0.8.7] - 2026-09-26

### 修复

- **心流 + 打字机滚轮向下滚动一段距离后被弹回光标行**（用户实测反馈）：滚动进入未测量区域会触发 CM 测量，内容层尺寸变化被动触发打字机重锚（ResizeObserver / `geometryChanged`），拉回光标行——违背「用户滚动绝不干预」规则。现引入**用户滚动冷却**（800ms 窗口内抑制被动触发，锚定自身的程序化滚动经标记排除），输入 / 点击定位的强制重锚不受限（滚动后输入仍会回到锚点，已回归验证）。
- **Alt+W 进入心流后光标失焦**（用户实测反馈：需点一下才能开始编辑）：进入心流的 wysiwyg 重配置 / 顶栏卸载会使编辑器失焦。现在进入心流后自动聚焦编辑器。

### 新功能

- **打字机开关快捷键与状态提示**（用户提出）：`Alt + T` 直接开关打字机模式（关 ↔ 上次使用的高位 / 低位，本地记忆恢复；编辑视图内）；编辑卡顶栏新增打字机按钮（激活时 accent 高亮），心流 / 专注右下角状态区新增第四个模式图标（打字机，展示实际生效形态——心流内设置关闭也按低位推导高亮）。

### 优化

- **打字机标识改用字母 T**（用户实测反馈）：Aim 十字准星图标与「定位笔记」按钮重复，顶栏按钮与状态区图标同步改为加粗字母 **T**（尺寸与图标一致，激活态同样随主题色高亮）。
- **设置页说明文字移至选项下方**（用户偏好）：选项右侧的浅灰说明改为换行显示在控件下方、与控件左对齐（102px 缩进，与既有的下方说明样式一致），整页版式更整洁；设置 → 通用 / 插件各分组同步生效。

## [0.8.6] - 2026-09-26

### 新功能

- **心流引用与速览增强**（FR-2.9.10，用户实测反馈，需求与设计见 [2026-09-25_flow-reference-enhance/](ai/requirements/2026-09-25_flow-reference-enhance/flow-reference-enhance.md)）：
  - **Esc 分级消费**：关闭悬浮预览时 `[[` 补全面板保持打开（此前事件继续下沉、补全被连着关掉），再按一次收起补全、再按退出心流——应用级 Esc 回退逐级消费；
  - **`[[` 扁平模糊匹配**：补全查询不含 `/` 时改为全库按名字片段匹配（不用再记完整路径逐级进目录），detail 显示所在目录，库内同名笔记插入完整路径消歧，上限 30 条；含 `/` 保留原逐级导航；
  - **搜索结果键盘导航与预览**：全局搜索框 `↓` / `↑` 移动高亮（循环 + 滚动跟随）、`Enter` 打开高亮项、`Alt+Enter` 关闭搜索框并以悬浮预览查看（经 app store 握手复用心流快速查阅的悬浮预览管线）；鼠标悬停同步高亮。
  - **悬浮预览「插入引用」**：补全预览态的悬浮预览头部新增「插入引用」按钮（快捷键同 Alt+Enter）——把光标前未完成的 `[[xxx` 替换为完整引用 `[[路径]]`，插入即收回预览继续写作；
  - **扁平模式并入文件夹候选**：输入文件夹名字片段也会列出文件夹（`dir_01/`，选中进入逐级导航），恢复 v0.8.3 的目录浏览能力；支持 `[[/` 从库根开始逐级浏览。
  - **底色过渡**：心流写作底色新增「底色过渡」开关（设置 → 通用 → 心流模式）——写作栏两侧各 15% 栏宽的过渡区，底色向两侧逐渐变透明；渐变的实心段在栏边前即达全浓度，编辑区与过渡区之间不再出现明显的浓度分界（四轮实测反馈）。
- **修复：子目录笔记的补全预览报 ENOENT**（FR-2.9.10 二轮实测反馈）：扁平补全的唯一名候选项只带叶子名，预览按库根路径找文件失败。现候选项携带完整相对路径（`notePath`），预览与插入引用均取真实路径。
- **修复：插入引用括号缺失 / 多余（FR-2.9.10 三轮实测反馈，两个案例）**：closeBrackets 会在输入 `[[` 时自动补出成对 `]]`，两条落成路径的括号语义不同、需分别处理——① **Enter 直接接受**（替换范围在 `[[` 之后）：光标后的 `]]` 就是闭合符，保留它、只插笔记名；无闭合时才补 `]]`（此前误吸收删除，产出 `[[名字` 缺右括号）；② **预览后插入**（替换范围含 `[[` 起点到光标）：插入完整 `[[路径]]` 并吸收光标后紧邻的残留 `]]`（此前插入裸名字，把 `[[` 一起吃掉——前后都没了 `[]`）。两条路径均已落盘断言验证（`[[子目录/深层]]` / `[[首页]]` 恰好一对括号）。
- **修复：搜索结果 Alt+Enter 行为错乱**（FR-2.9.10 三轮实测反馈）：① `Enter` 未加 `.exact`，Alt+Enter 同时触发了「打开高亮结果」；② 预览态 Alt+Enter 的插入兜底落给 CM 默认 Enter（插入换行）。现预览态 Alt+Enter 三种情况都消费按键：补全活动整段替换 / 补全已关光标处插完整引用 / 目标为当前笔记自身仅收起预览不插入（「插入引用」按钮同口径）。

- **行插入快捷键**（用户提出）：`Ctrl + Enter` 在当前行下方插入一个空行、`Ctrl + Shift + Enter` 在上方插入——不论光标在行内什么位置，插入后光标移到新行行首（典型场景：`[[` 补全落成引用后光标停在行中，直接换行写下一行）。补全面板打开时 `Ctrl + Enter` 仍为「接受补全」。

## [0.8.5] - 2026-09-25

### 修复

- **冲突解决接口的路径穿越漏洞（安全）**：`gitService.resolveConflict` 把渲染端传来的 `filePath` 直接 `path.join(vaultPath, filePath)` 写盘，未过 `resolveWithin` 校验，可借 `../` 越权写库外任意文件。现于方法入口加库内路径守卫（越界直接失败），新增单测覆盖。技术债评审（[tech-debt-2026-09-25.md](ai/tech/tech_architecture.md)）发现。
- **从搜索结果打开笔记污染「常用」列表**：`App.vue` 把整篇笔记内容误当显示名传入 `editor.openNote`，随 `addRecent` 持久化后常用卡片标题渲染为整篇内容。现改为传去扩展名的文件名（与树节点 / 反向链接一致），预读仅用于失败提示。技术债评审发现。
- **搜索索引不随文件变更更新**：watcher → 索引链只接了双链索引，搜索索引仅在启动与手动重建时全量刷新——两次重建之间新建 / 改名 / 外部修改 / git 同步拉取的文件搜不到。现把 `search.updateFileIndex` 接入 watcher 变更回调（与双链索引同模式）。技术债评审发现。
- **git 同步无按库互斥**：手动同步与 autoSync 定时器可能并发对同一仓库执行，撞 `.git/index.lock` 时把内部错误直接暴露给用户。现按库串行排队（后到者等前一轮完成后再跑），新增并发单测。
- **冲突解决三向视图永远「加载冲突内容中」**（用户实测反馈）：`getConflictContent` 用 `git show :file` 取当前内容，而冲突路径在 index 中没有 stage 0，该命令必然报 unmerged，三方内容永远拿不到。现改为直接读工作区文件（含冲突标记）；共同祖先改由 merge-base 实时计算（原实现引用的 `MERGE_BASE` 并非有效引用名，base 面板一直为空）。加载失败时对话框给出错误态与「重试」按钮，不再永远转圈。
- **「接受本地版本」写入了远端内容**（用户实测反馈）：git 的 ours / theirs 语义随操作类型**反转**——merge 冲突里 ours=HEAD=本地，而 `pull --rebase` 的冲突中 HEAD 是被变基到的**远端上游**、本地提交在被重放侧（REBASE_HEAD）。现按变基状态对调本地 / 远端取值，「接受本地版本」写回的真正是本地内容；新增端到端单测锁定该语义。
- **「继续同步」报错 Configuring core.editor is not permitted（用户实测反馈）**：continueRebase 用 `-c core.editor=true` 跳过变基编辑器提示，被 simple-git 默认拦截。现于 git 选项显式开启 allowUnsafeEditor（配置值由应用自身提供，非外部输入）。
- **冲突未处理完时再点同步报「似乎已有一个 rebase-merge 目录」**（用户实测反馈）：现同步前检测变基进行中——仍有未解决冲突则带出列表**重新打开解决对话框**；冲突已全部标记解决则就地续完本轮同步（自动补跑变基 + 推送，用户此前「解决完直接关了对话框」的场景自愈）。
- **冲突对话框「使用此版本」不触发解决**（用户实测反馈）：合并视图的「本地版本 / 远端版本」tab 里的「使用此版本」此前只把内容填进编辑框，与顶部的「接受…版本」形成两套平行机制——点了它「已解决」计数与「继续同步」毫无反应。现改为**真正以此版本解决冲突**（与顶部按钮同效）；「使用编辑内容」更名「以此内容解决」，刷新按钮加悬停说明。
- **冲突对话框编辑区只剩约 3 行高**（用户实测反馈）：body 高度规则写在 scoped 样式里、而 class 挂在 el-dialog 根元素上没有组件 scoped 属性，规则实际从未命中。改为非 scoped 全局样式，编辑区撑满（实测 70vh 内 407px）。
- **冲突文件列表没有选中效果**（用户实测反馈）：高亮用的 `--accent-light` 变量从未定义过，`var()` 静默失效。改用已定义的 `--accent-soft`（反向链接胶囊的两处同款失效一并修正）；已解决的文件改为绿色「已解决」标签 + 文件名删除线，状态一目了然。
- **「继续同步」遇到后续提交的新冲突**：变基逐提交重放，后续提交可能出现新的冲突——现刷新冲突列表并重置解决状态、提示继续处理（此前停留在旧列表上）。
- **弹出「解决冲突」后应用窗口右侧出现滚动条**（用户实测反馈）：el-dialog 默认 15vh 顶边距 + 50px 底边距，加上 70vh 的 body 后总高恰好溢出视口。现收敛为 5vh 顶边距并按视口钳制 body 高度，弹窗不再把遮罩层顶出滚动条。
- **合并视图图例的三种颜色在内容区不显示**（用户实测反馈）：合并视图此前只是纯白底 textarea，「本地 / 远端 / 冲突标记」三个图例色对应的着色从未实现（且图例色块本身用了极浅硬编码色、形同隐形）。现为合并视图实现**逐行着色**（高亮层叠在透明输入区下、滚动同步）：冲突标记行红底加粗、本地内容行蓝底、远端内容行绿底，图例色块改用主题变量清晰可见。区段与颜色的对应关系按**内容自证**定向（与 ours 实际内容比对）——变基冲突里 HEAD 段是远端、merge 里才是本地，按标记位置猜必然染反，比照图例又会误导（深浅主题各有一套取值）。

### 优化

- **git 同步挂起期的变更补偿补全**：resume 后此前只重建双链索引——同步拉取的文件对渲染端不可见、目录树可能保持陈旧。现同时全量重建搜索索引，并对所有库重发一次 `fs:changed`（空 paths：目录树按库全量重扫，编辑器按 paths 过滤不受影响）。
- **watcher 防抖增加 max-wait 兜底**：持续写入时尾沿计时器被不断重置，通知可能无限期推迟；现超过 2 秒强制发出一批。
- **应用元数据持久化底座加固（JsonStore）**：写入补 fsync（崩溃不留半截文件）；读取与默认值**深合并**（新版本给嵌套默认值加键不再被旧数据整体顶掉，settings 的 sidebarMenus 等嵌套结构受益）；文件损坏时保留 `.bak` 现场再回退默认值（不再静默丢数据）；新增 `schemaVersion` + `migrate` 钩子，旧版设置键升级收口到统一迁移链（autoSyncEnabled → autoSyncMode 兼容已迁入）。新增 6 项单测。
- **`settings:set` 增加键白名单**：渲染端传来的未知键不再被合入设置落盘，避免升级换名后幽灵键永久留存。
- **冲突解决对话框风格统一**（用户实测反馈）：解决操作按钮由高饱和实心蓝 / 绿 / 黄改为浅色描边样式，「放弃所有更改」改为红色描边——与应用整体设计语言一致。

### 新功能

- **心流写作底色**（用户提出，FR-2.9.8 扩展）：设置 → 通用 → 心流模式新增「写作底色」开关与五种柔和颜色（米黄 / 淡绿 / 淡蓝 / 淡粉 / 淡灰，深浅主题各有一套取值）——为心流模式的写作栏涂一层通到底的底色（含行号槽、随栏宽变化），帮助视线聚焦；仅心流模式内生效，退出即消失。实现上给写作栏两个直接子元素（行号槽 + 内容层）涂色，自然随文档伸缩与滚动；第一版的滚动容器 `background-clip: content-box` 方案因绘制区域与滚动几何脱节而废弃（详见 [flow-mode 设计文档 10.10](ai/requirements/2026-09-23_flow-mode/flow-mode_design.md)）。

## [0.8.4] - 2026-09-25

### 优化

- **侧栏区块标题布局：「+」紧贴菜单名，计数角标统一吸到行尾最右**（用户实测反馈）：标签 / 笔记库区块标题的「+」固定紧跟菜单名（间距 6px），计数角标统一吸到行尾最右——数字不再被挤离右缘，0 计数时「+」跳位的问题（2026-09-24 登记）一并销账。实测常用 / 标签 / 笔记库三行角标右缘完全对齐。

## [0.8.3] - 2026-09-25

### 修复

- **预览 / 悬浮预览的初始位置不正确**（用户实测反馈）：编辑区滚动到中部后打开预览，预览停在文档开头（同步滚动功能本身正常，滚动一次后即对齐）。根因是行级同步全靠滚动事件驱动，而预览初次挂载从顶部起步、编辑器也没滚动——双向都收不到事件，没有任何同步发生。修复：预览出现的三条路径（分栏开关打开 / 悬浮预览呼出 / 打开笔记且预览可见）**主动同步一次**到编辑器当前可视行。
  - 同步前置位防回环 guard（source=editor）：写入预览 `scrollTop` 会触发预览 scroll 事件 → 预览→编辑器方向同步 → CM 滚动，实测会把编辑器拖到完全不同的位置（预览挂载初期布局未稳、行号读数偏差大）；100ms 防回环窗口让该回环事件被丢弃。
  - 顺带修复 `syncToLine` 对**文档尾部超高块**的块内比例折算：目标块是最后一个块时此前固定按 200px 回退，长段落 / 长代码块场景同步位置严重偏早（实测停在 4.6% 而非 ~87%）；现回退到块自身渲染高度。
- **心流下反向链接胶囊无法点开**（与下文「hover 显现」为同一未发布周期）：胶囊 hover 显现用的外扩感应区（`::after`，`inset: -12px`）是定位元素、绘制在胶囊按钮之上，点击全部落在感应区上（事件 target 为根 div），按钮的 `@click` 永远收不到——表现为「hover 能显现、点击无响应」。修复：胶囊提升绘制层级（`position: relative; z-index: 1`），感应区的悬停命中不受影响；点编辑区收回、`Esc` 收起等既有交互不变。

### 优化

- **心流下切换所见即所得模式增加弹窗提示**（用户实测反馈）：顶栏隐藏后，切换结果此前只有右下角状态区图标的被动高亮，不明显且需要移动视线焦点。现于顶栏隐藏形态（心流，或专注且开启「隐藏顶栏」）下，`Ctrl+E` 或工具栏按钮切换时于**屏幕正中**弹出大字提示「所见即所得模式：开 / 关」——自绘浮层（Teleport 到 body 的 `fixed` 居中，不拦鼠标），字号约为常规提示的 2 倍（28px）、浅绿色底 60% 不透明、文字用与底色同系的中深绿（新增主题变量 `--success-bg` / `--success-text`，深浅色主题同值），2 秒自动消失、重复切换重置计时；非顶栏隐藏形态维持原行为不弹窗（顶栏按钮与 tooltip 本身可见）。判定收口为 app store 新增 `topbarConcealed` getter（与右下角状态区的显示共用同一条件）；提示挂在唯一的用户切换入口 `toggleEditorMode()` 上——心流预设进入 / 退出对编辑形态是直接赋值，不会误弹。

### 新功能

- **心流快速查阅：补全面板里预览笔记**（FR-2.9.8 扩展，用户提出）：心流下输入 `[[` 后，补全面板选中一篇笔记按 **Alt+Enter**，将以悬浮预览打开该笔记（标题显示「预览：笔记名」）——不切换当前笔记、不插入文本、补全面板保持打开；看完点编辑区即收回（悬浮预览的「一瞥」语义），继续写作。悬浮预览中点击双链会**在该悬浮预览内导航**（不切走），形成「顺藤摸瓜看一圈，回来接着写」的动线。补全的 Enter（接受插入）行为不变。
- **心流下反向链接胶囊默认隐藏**（用户提出）：鼠标移到右下角感应区（胶囊外扩 12px）才显现，写作零干扰；弹层展开时保持可见。

- **心流 / 专注模式下右下角状态区**（FR-2.9.5 扩展，用户提出）：顶栏隐藏后，原右上角的 Git 状态标志无处可看——现于编辑卡右下角渲染**长条形状态区**（胶囊形、有底色与描边以区别于正文，85% 不透明度），内容从左到右：**Git 状态**（分支图标 + 等宽分支名 + 未推送 ↑ / 未拉取 ↓ + 未提交修改 + 同步中转圈；当前库是 git 仓库即显示）→ 保存状态**小圆点**（未保存淡显 / 保存中脉冲 / 已保存一闪 / 失败红色；静置隐去——写作专注优先，保存事件只轻点一下）→ **编辑模式三图标**（所见即所得魔法棒 / 心流咖啡杯 / 专注全屏，各自激活时 accent 高亮——顶栏隐藏后仍能确认当前处于哪些模式）。当前库是 git 仓库即显示（含未关联远端的本地库）；悬停唤出顶栏时状态区自动淡出（完整状态在顶栏里，避免同屏双份）。**反向链接胶囊保留在状态区上一行**（顶栏隐藏时自动上移让位），两者不再重叠。

## [0.8.2] - 2026-09-24

### 修复

- **进心流后光标不停在锚点线上（FR-2.4.14 / FR-2.9.8）**：按 `Alt+W` 进入心流后，光标跑到编辑区上方，而不是设置的高位 / 低位锚点线。排查发现打字机的重锚**只有两个入口**——用户事件与 `ResizeObserver`（且只观察滚动容器），而插件新建时只写留白、不锚定；于是凡「**滚动容器尺寸不变**」的几何变化都会静默漏掉：这类变化（字号调整、切进心流后的所见即所得重排、块级 widget / 公式重新测量、图片与字体异步加载）既没有 CM 事务、也不改容器盒子，CM 与 RO 都不会知道。实测复现与修复对照：打字机生效时把字号 15 → 26（内容层高度变、滚动容器 561×765 不变），光标从锚点 0.512 漂到 **1.78**（视口外）且 4 秒内不回来，修复后自动回到 **0.52**。现补三重保障：① 插件挂载即排一次锚定（进心流 `off → bottom` 会新建插件）；② `ResizeObserver` 追加观察**内容层** `contentDOM`；③ `update()` 增加 `geometryChanged` 分支（CM 语义含「编辑器 / 内部元素尺寸变化」，**不含滚动**，不违反「用户滚动绝不干预」）。另外：`anchor()` 原先在 `coordsAtPos` 返回 `null`（光标不在已渲染范围）时直接放弃，锚定会永久失效——现补一次「请 CM 滚进视野后再重试」；同时把**留白提升为锚定前每次校验的不变量**（`applyPadding` 写完先比较、`anchor()` 开头先补写）——留白是锚定成立的前提（光标上方内容不足时，靠它把首行连同光标推到锚点线，否则「所需滚动位置为负」被夹到 0，光标就停在靠上处），这样一来任何导致留白缺失或过期的路径都会在下一次锚定时自愈。回归实测：进心流两态 0.811 / 0.511（目标 0.8 / 0.5）、近一页与短于视口的笔记同样 0.811 / 0.511、滚轮上滚后 1.5s 位置未被拉回、打字机生效时输入空格 / 数字 / 字母均正常插入且输入后回到锚点。详见 [flow-mode_design.md](ai/requirements/2026-09-23_flow-mode/flow-mode_design.md) 第 10.9 节。

## [0.8.1] - 2026-09-24

### 修复

- **编辑器内打不出空格与数字 0-9（v0.8.0 回归，严重）**：表格尺寸提示态（FR-2.4.20）的键位把 `Space` / 数字 `0`-`9` / `Enter` / `Escape` 都声明了 `preventDefault: true`，而 CM6 的语义是**该标志只在命令「未处理」（返回 `false`）时才生效**（并顺带把按键标记为已处理）——于是提示态没打开时，命令返回 `false` 反而触发了阻断：**空格与数字完全打不进编辑器**（字母正常；回车也正常——换行由 CM 自己的键位插入，不依赖浏览器默认行为）。现按同文件 `Tab` 的既有修法去掉这几个绑定上的 `preventDefault`：命令返回 `true` 时 CM 在事件分发里自会 `preventDefault`（尺寸输入不会漏进正文），返回 `false` 时按键正常放行。**实测两态**：未激活时按空格 / `5` / 字母各插入 1 个字符（修复前空格与数字均为 0）；激活时输入「5 空格 6」正文零写入、浮层实时回显「5 行 × 6 列」、按空格插入 6 列表格、`Esc` 取消不插入。已排查渲染进程其余 `preventDefault: true` 绑定（全在 `Mod-*` 组合键、命令均无条件返回 `true`），无同类问题。

### 优化

- **标签区可折叠（FR-2.6.11）**：侧栏「标签」区块标题新增展开箭头（与「笔记库」区同款交互与视觉，标题文字列与其他区块保持同一列），标签多时可一键收起——实测 8 个标签收起后为下方区块腾出 260px，不再把「断链引用 / 回收站 / 笔记库」挤出可视区。收起态保留标签数量角标与「+」新建按钮；**收起时新建标签会自动展开**（否则新标签建完就看不见）；展开状态持久化（localStorage `trace.tagSectionOpen`），重启与重载后保持。区块标题的点击行为不变（仍是打开标签网格）。设计稿原本就画了 `▼ 标签 (3)` 的箭头，本次补齐。

- **侧栏区块标题「+」按钮对齐**：标签区与笔记库区的「+」按钮原先各用一套样式（标签区 `.side-section-add` 实测 17.3px 宽且带 4px 额外左边距，笔记库区 `.row-btn` 固定 22px），于是两个加号错开约 4.7px、计数角标也差 0.7px。现统一为同一按钮样式，两区块的计数角标与「+」按钮**逐像素对齐**（实测角标 left 219.2 / right 238、按钮 left 244 / right 266 两组数值完全一致），悬停反馈随之统一；失去引用的 `.side-section-add` 样式已删除。

- **侧栏搜索按钮移至标题行右端**：原先搜索图标紧跟「Trace 笔迹」标题，现靠右对齐（标题与按钮之间的空余由 `margin-left: auto` 吸收），位置与「标签 / 笔记库」区的 `+` 按钮同列——实测按钮右边界 266，距侧栏右边界 16px（与 `+` 按钮的右边界一致），不再挤在标题右侧。

## [0.8.0] - 2026-09-24

### 新功能

- **表格插入：尺寸输入提示（FR-2.4.20）**：`Ctrl+T` / 工具栏「表格」按钮不再直接插入固定表格，而是先弹出**跟随光标的浮层**——实时回显将插入的行列数（默认 `2 行 × 2 列`）、操作提示与默认行为预告。输入「行 列」（如 `5 6`）后空格或回车即插入对应尺寸（只给行数时列取默认 2）；浮层**不设倒计时**——一直等用户输入或按空格 / 回车（未输入时直接空格 / 回车 = 按默认尺寸插入），`Esc` / 点击他处 / 失焦取消（不写入正文）；范围行 1–50、列 1–20，超限红字提示并按上限处理
- **表格内 Tab 跳转（FR-2.4.21）**：光标在表格内时 `Tab` / `Shift+Tab` 逐个单元格前后跳转（行优先，从表头开始）并选中该格内容，末格 `Tab` 自动追加一行、首格 `Shift+Tab` 离开表格；表格外的 `Tab` 行为不变（仍移动焦点），补全浮层打开时让位

- **编辑位置（FR-2.4.18）**：设置 → 通用 → 编辑器新增「编辑位置」——「从头开始」（默认，与现状一致）/「从尾部开始」。选「从尾部开始」后，打开任何笔记（树 / 网格 / 收藏 / 常用 / 搜索 / 双链 / 重启恢复）光标直接落在文末并滚到文末，适合日记、流水记录等追加式写作；行为与「上次编辑位置」无关，重命名笔记与外部修改重载（git 同步 / 外部编辑）都不会把光标挪走
- **光标跳转文件头 / 尾（FR-2.4.19）**：编辑器内 `Ctrl+Shift+H` / `Ctrl+Shift+E` 跳到文件开头 / 末尾（主键盘区键位，笔记本上不必再 Fn + Home / End），源码与所见即所得模式均可用，与既有 `Ctrl+Home` / `Ctrl+End` 并存

- **标题层级快捷键（FR-2.4.15）**：编辑器内 `Ctrl+1` … `Ctrl+6` 把当前行设为对应级别标题，`Ctrl+0` 清除标题；已是目标级别时再按一次即去掉标记（开关语义），多行选择时逐行生效，单步可撤销。工具栏原「H1 / H2」两个按钮升级为「标题」下拉（一级 ~ 六级 + 清除标题，菜单项右侧显示快捷键），六个级别同等可达。
- **表格插入（FR-2.4.16）**：工具栏新增「表格」按钮（`Ctrl+T`）——在光标处插入 GFM 表格模板并选中首列表头以便直接改名（插入尺寸由 FR-2.4.20 的尺寸输入提示浮层驱动，不输入即默认 2 行 2 列）；行中插入时自动换行，保证表格独占行块。
- **折叠按钮（层级 / 块内容）优化（FR-2.4.17）**：行号列右侧的折叠箭头由 CM 默认文本字形（`⌄` / `›`）改为**与应用图标语言一致的线性箭头**（16px），热区 20×20、悬停圆角背景与正文色；折叠行为、出现场景与折叠后的「⋯」占位符不变。为拿到 `foldGutter` 的配置入口，编辑器改为自有装配 `traceSetup()`（清单逐项对齐 `basicSetup`，仅替换折叠按钮）。

- **打字机模式（心流模式 P1，FR-2.4.14）**：设置 → 通用 → 编辑器新增「打字机模式」，可选高位（光标垂直居中）或低位（光标锚定在距底边 20% 处）；输入时表现为「文字移动、光标不动」。用户滚动查看前文时不干预（下次输入回到锚点），拖选中与中文输入法组词期间不重锚；源码模式与所见即所得模式均可用。实现见 `src/renderer/src/lib/typewriter.ts`，需求与设计见 `ai/requirements/2026-09-23_flow-mode/`。

- **心流模式（P2，FR-2.9.8）**：一键进入沉浸创作——`Alt+W` 或编辑卡顶栏咖啡杯按钮，自动隐藏侧栏 / 迷你导航条 / 顶栏、切所见即所得、开启打字机（「打字机模式」设为「关闭」时心流内按低位生效，已选高位 / 低位则沿用）、启用写作栏宽与保存指示；`Esc` 一键退出并**还原进入前的界面状态**（侧栏 / 专注 / 预览 / 编辑形态，快照语义）。
  - **写作栏宽**：三档（窄 32 / 中 42 / 宽 52 字符），文本列含行号整体居中，卡片背景仍铺满；
  - **保存指示**：编辑卡右下角极微弱圆点（有未保存变更淡显、保存中脉冲、保存成功一次闪烁、失败 / 外部冲突红色常亮），不占布局、不打断输入；
  - 心流内保留 Ctrl+F 全局搜索、Ctrl+N 新建、Alt+B 呼出浮层侧栏；
  - 设置 → 通用新增「心流模式」分组（写作栏宽）。

- **心流模式回车音效（P3，FR-2.9.9）**：回车插入换行时播放程序合成的机械键盘音（Web Audio 合成，每次带轻微随机变化避免机械重复感）。**默认关闭**；设置 → 通用 → 心流模式提供开关、**音色选择**与音量（默认 60%，默认音色「复古打字机」）；
  - **四种音色**（均按收到的实录音效实测声学特征重建，**不随包音频文件**）：**复古打字机**（默认，三段结构：① 按键「咔」——宽带脉冲 + 四处微冲击 + 字锤/纸卷共振 + 延迟 12ms 的机体「咚」；② 70ms 起的**推回车棘轮**——约 35 颗齿、间隔 9ms 渐密到 6ms、振幅渐强；③ 240ms 起的**回车铃「叮」**——2742/6527/9700/11449Hz 四个分音、余振约 0.7s；整段 1.0s、末尾 60ms 淡出）、**推回车（棘轮）**（0.3s，即上者棘轮段单独成声）、**回车铃**（0.8s，即上者铃单独成声）、轮换（三者依次交替，避免长时间写作的重复感）；同种音色再次触发会先停掉上一声，异种可共存；
  连续回车 120ms 限流；仅心流模式内生效；粘贴多行不发声；关闭开关即释放音频上下文。**「连续换行屏蔽音效」默认开启**：连按回车加空行（间隔 < 800ms）时只有第一次发声，避免音效叠在一起。零音频文件、零安装包体积、无版权问题。

### 优化

- **所见即所得：行内标记的源码露出粒度**（FR-2.4.13）：加粗 / 斜体 / 删除线 / 行内码原先按「光标与整个片段相交」回落源码，于是在 `~~删除~~` 的内容末尾打字时，两端的 `~~` 与删除线同时可见（看着像删除线下面挂着波浪号）。现改为**只有真的贴到定界符才露出**——光标在内容里（含两端、输入过程中）始终保持渲染；紧贴标记外侧、进入标记字符之间（如 `~~` 两字之间）、或选中标记（含拖选跨越）才显示源码。定界符仍可正常编辑（方向键进入标记后即可删除）。链接 / 双链 / 图片 / 公式等仍是「光标进入即回落」，便于点击后直接编辑

### 修复

- **所见即所得：行号 / 行号高亮与正文错位**（块级 widget 的几何记账，FR-2.4.13）：块级公式、表格、水平线等 widget 的垂直间距原先由 CSS 外边距提供，而 CM6 的行高记账只取 widget 元素的 border-box、不含外边距——每个块级 widget 之后的所有行号 / 行号高亮整体上移并逐块累加（实测公式块后 +15px、表格后再 +12px，累计 27px），同时影响预览行级同步与「跳转到指定行」的落点。现改为容器 `padding` + `display: flow-root`（间距落在盒内、可被测量），视觉间距数值与原先一致；顺带修正：渲染 HTML 的换行在编辑器 `break-spaces` 下会变成空行、预览卡片的 `padding` / `max-width` 被 widget 误继承（水平线上下间距不对称、表格被限宽）
- **所见即所得：列表 `-`、引用 `>` 未隐藏，任务复选框不可见**（FR-2.4.13）：列表标记与引用标记此前根本不生成装饰（`>` 是 v0.6.0 的既定边界，本次按实测反馈改为隐藏）；任务复选框的样式被写在 `.markdown-body` 作用域内，而编辑器内容区没有该祖先，复选框退化成无尺寸空元素——表现为 `[ ]` 被吞掉、方框却没出现。现：无序列表标记渲染为「•」、有序列表保留编号、任务列表的 `- ` 由复选框取代、引用 `> ` 隐藏；标记类装饰统一按「光标所在行整体回落源码」判定
- **所见即所得：带 frontmatter 的笔记几乎完全失效**（FR-2.4.13，复现上述两项时发现）：frontmatter 剪枝写成了「区间相交」判定，而 `Document` 根节点必然与 frontmatter 相交 → 语法树在根节点被整棵剪掉，标题 / 列表 / 引用 / 表格 / 行内样式全部退回源码（只有块级公式还渲染）。修正为「节点完全落在 frontmatter 区间内才跳过」，对带标签（frontmatter 存 tags）的笔记影响面 100%

- **表格内 `Tab` 绑定误吞按键**：给 `Tab` / `Shift-Tab` 绑定声明 `preventDefault` 后，CM 会「无条件」阻断默认行为（即使命令返回未处理），导致表格外按 `Tab` 也无法移动焦点；现改为只在命令真正处理时由 CM 自动 `preventDefault`
- **点击渲染态表格后 `Tab` 失效**：所见即所得下点击表格会把光标落在块边界，反向偏置的语法树解析落到相邻节点、判定为「不在表格内」；现按边界认领
- **全局快捷键判定过宽导致 `Ctrl+Shift+E` 误切编辑模式**：`App.vue` 的全局 Ctrl 系处理器只按字母匹配、未排除 Shift，同一按键会既跳文件尾又切模式（新增 FR-2.4.19 时暴露）；现补 `!e.shiftKey` 判定
- **Ctrl+F 会同时弹出编辑器原生查找面板**：`MarkdownEditor` 里禁用它用的绑定写作 `run: () => false`（CM 语义为「未处理」，会继续落到 builtin 的 searchKeymap），故查找面板照常弹出并抢走焦点；改为 `Prec.high(...)` 提升应用级键位优先级并 return true 消费该键（CM 只 preventDefault、不阻断冒泡，全局搜索仍会打开）
- **Esc 分级回退会一次退两级**：原先浮层侧栏、悬浮预览、心流退出分散在 App 与 EditorView 两个 window 监听器里，彼此会看到对方刚改过的状态（实测关浮层的同时把心流也退了）；现统一由 App.vue 的单一入口处置，并改用**捕获阶段**判断（早于 Element Plus 对话框自身的 Esc 处理，保留「有模态时 Esc 归模态」的语义）
- **退出所见即所得后预览分栏状态丢失**：该联动原在 EditorView 的 watcher 里，组件重挂载会丢掉「进入前分栏状态」的记忆，且与心流的进入 / 退出互相覆盖（实测退出心流后预览未能还原）；现移入 app store 的 `setEditorWysiwyg` 统一维护

## [0.7.0] - 2026-09-22

### 新功能

- **Windows 自定义标题栏（WCO）+ 毛玻璃恢复**（需求与设计见 `ai/requirements/2026-09-22_custom-titlebar/`）：
  - Windows 窗口改用 Window Controls Overlay：系统三键与 Win11 贴靠布局保留，顶部新增应用名标题栏条（可拖动、双击最大化）
  - 「窗口效果」在 Windows 恢复完整功能：Mica / Acrylic 材质（`backgroundMaterial`，需 Windows 11 22H2+，旧系统自动降级为仅透明度）
  - 玻璃开启时卡片背景转半透明透出材质（文字层不透明，可读性不降级）；深浅主题切换时标题栏按钮配色自动跟随
  - macOS / Linux 窗口行为不变（平台矩阵严格限定 win32）

## [0.6.1] - 2026-09-22

### 优化

- 设置项「窗口透明度」更名为「窗口不透明度」：该设置实际语义为数值越大越不透明（50 半透明 / 100 完全不透明），原名与行为相反易造成误解

### 修复

- **列表项内块级公式（`- $$…$$`）**（已知局限毕业，规避 texmath 上游缺陷）：
  - 句中的 `$$…$$`（如「- 结论：$$E=mc^2$$」）不再渲染为整行居中的展示公式截断句子，改以行内公式呈现——预览 / 导出 / 所见即所得三处一致
  - 修复同一条列表项内 `$$a$$ 和 $$b$$` 时**公式后的尾随文字被静默吞掉**的显示缺陷（texmath 块规则整行吞并所致，现以块规则守卫拦截并交还行内解析）
  - 所见即所得编辑器：修复列表项内多行块级公式的闭合 `$$` 被误认为起始、把后续多个列表项错配成一个巨型公式的问题；带列表标记的单行公式以行内呈现

## [0.6.0] - 2026-09-22

### 新功能

- **所见即所得编辑模式（Live Preview，Obsidian 式）**（FR-2.4.13，需求与设计见 `ai/requirements/2026-09-21_wysiwyg/`）：
  - 编辑器顶栏新增魔法棒按钮，`Ctrl+E` 随时切换源码 / 所见即所得；设置 → 编辑器新增「默认编辑模式」
  - 非光标区域按渲染形态呈现：标题大字、加粗 / 斜体 / 删除线、行内码、引用样式、可点击勾选的任务列表、表格、行内 / 块级 LaTeX 公式、图片、双链（断链删除线、`Ctrl+点击` 打开、同名消歧）、链接（`Ctrl+点击` 打开）、内嵌 HTML、frontmatter 折叠为一行；光标所在区域回落显示源码，移开即重新渲染
  - 进入所见即所得自动收起预览分栏，切回源码恢复原分栏；悬浮预览、专注模式不受影响
  - 文件始终为纯 Markdown——所见即所得只是编辑器呈现层，保存产物与源码模式完全一致
  - 中文输入法组词期间冻结装饰重渲染，避免候选框错位 / 拼音重复上屏
  - 净化管道抽取 `sanitizeHtml` 供预览 / 导出 HTML / 编辑器三处共用

## [0.5.1] - 2026-09-21

### 优化

- 插件设置页区块重排：「已安装的插件」移至「插件市场」之前——市场插件增多时，已安装列表不再被挤出首屏

### 修复

- **Linux 移除窗口底部圆角**：浅色壁纸下内容区底部圆角缺口仍会残留系统合成器的方形直角轮廓（深色壁纸正常，疑似系统侧限制、应用侧无法彻底消除）——Linux 平台一律去掉底部圆角规避；Windows / macOS 不受影响（Windows 不透明窗口本就不启用内容区圆角）

### 构建

- 插件类型包 `trace-plugin-api` 改为 CI 自动发布（npm Trusted Publishing / OIDC，免 token）：应用打版本标签时，CI 比对 `packages/plugin-api` 本地版本与 npm 线上版本，不同才发布（自带 provenance）；补齐 package.json `repository` 字段（provenance 校验要求）。首次使用前需在 npmjs.com 登记一次 Trusted Publisher，见 `guides/plugin-development.md` 第 9 节

## [0.5.0] - 2026-09-21

### 新功能

- **插件系统 M1：进程隔离 + Tier 1 能力 API + 权限确认**（设计见 `ai/requirements/2026-09-08_plugin-system/`，实施记录见其第 10 节）：
  - **进程隔离**：每个启用的插件运行在独立 `utilityProcess` 中，插件崩溃只影响自身——守护自动按 1s/2s/4s/8s 指数退避重启，连续崩溃 5 次自动停用并标记错误；停用宽限 5s 超时强杀
  - **能力网关**：插件的全部能力调用按 manifest 声明的权限过滤，未声明权限直接抛错；`require` 白名单限制插件只能加载自身目录文件与 path/util/events 内置模块（自包含政策，禁 npm 依赖）
  - **Tier 1 API**：`ctx.notes.vaults/list/tree/read/write/create`（notes:read / notes:write 权限，写侧透传防覆盖 hash）、`ctx.on/off` 订阅 `note:saved / note:opened / vault:changed / sync:done` 事件（events 权限）、`ctx.notify`（notifications 权限）、`ctx.logger` 与 `ctx.registerCommand`（内置）；RPC 调用 5s 超时、单次内容 400 万字符上限、`vault:changed` 高频事件 300ms 合并
  - **权限确认对话框**：首次启用或 manifest 权限集合变化时，设置页弹窗逐项展示权限中文说明，确认后才激活（确认记录按插件 id 存应用数据目录）
  - **设置页增强**：插件行显示权限清单、运行状态、崩溃计数与命令按钮（点击运行命令）
  - **示例插件升级 v2**：覆盖 Tier 1 全部能力（读笔记统计字数 + 写入报告笔记 + 订阅保存事件 + 注册命令）
  - 文档：使用说明见 [guides/user-guide.md](guides/user-guide.md) 第 15 节；插件开发指引见 [guides/plugin-development.md](guides/plugin-development.md)

### 插件系统 M2（同版本随上）：.trace-plugin 打包导入导出 + 私有存储 + 设置页详情

- **插件包导入**：设置 → 插件 → 「导入插件…」选择 `.trace-plugin`（zip 格式）文件；解压暂存后弹窗预览（名称/版本/描述/权限清单 + 显著提示「未经 Trace 市场审阅」），确认后安装；同 id 重复导入为升级覆盖，原启用中的插件权限未变时自动重新激活，权限有变化则要求重新确认。解压带 zip-slip 与炸弹防护（总大小 ≤20MB、条目数 ≤2000）
- **插件包导出**：插件详情中「导出…」把已装插件打包为 `.trace-plugin` 分享
- **插件卸载**：详情中「卸载」= 停用 + 删除目录 + 清除权限记录与私有存储（红色确认）
- **插件详情**：设置页插件行「详情」显示运行状态、权限、崩溃历史（时间 + 退出码，最近 10 条）、最近日志（按插件过滤主日志）、私有存储占用
- **插件私有存储**（`settings:persist` 权限）：`ctx.storage.get/set/delete/keys` 宿主托管的插件私有 KV 存储（按插件隔离，随卸载清除；键 ≤200 字符、单值 ≤256KB、总量 ≤1MB）；示例插件升级 v2.1.0 演示（记录命令执行次数）

### 插件系统 M3（同版本随上）：声明式工具栏按钮 + 状态区 + 类型包

- **编辑器工具栏按钮**（`editor:toolbar` 权限）：插件在 manifest `contributions.toolbar` 声明 `{icon, title, command}`，编辑器工具栏自动出现按钮，点击执行对应插件命令——完全声明式，插件代码不碰 DOM；命令未注册或插件停止时按钮自动消失
- **侧栏状态区**（`ui:status` 权限）：`ctx.status.set/clear` 在侧栏底部显示一行文字（≤120 字符，如字数统计），每插件一条互不覆盖，插件停止自动清除
- **`trace-plugin-api` 类型包**：`packages/plugin-api/` 提供 `ctx` 完整 TypeScript 类型与 JSDoc（npm 发布待办）；示例插件升级 v2.2.0 演示工具栏按钮与状态区

### 插件系统 M4（同版本随上）：插件市场

- **插件市场**：设置 → 插件新增「插件市场」区块，浏览并一键安装市场插件（索引仓库 `imnot70/trace-plugins`，零自建后端）。安装管线 = 下载 Release 资产 → **sha256 与索引登记值比对（版本锁定）** → 复用本地导入管线（zip-slip/清单校验/权限确认）
- **更新检查**：市场安装的插件有新版本时，插件行显示「更新到 x.y.z」按钮；更新重走完整安装管线，权限新增时要求重新确认
- **下架机制**：索引删除条目后，已安装插件标记「已下架」（保留可用，仅停止更新推荐）
- **来源登记**：市场安装的插件记录来源（userData/market-installed.json），卸载时清除；本地导入的插件不参与更新检查
- **网络**：市场请求（索引拉取 + 资产下载）跟随设置中的 HTTP/HTTPS 代理，索引拉取带 ETag 缓存与 24h TTL，网络失败自动回退本地缓存
- 文档：插件上架流程见 [guides/plugin-release.md](guides/plugin-release.md)

## [0.4.6] - 2026-09-20

### 修复

- **修复 Windows 平台窗口无法移动 / 关闭、无圆角的严重问题**（v0.4.4 引入）：Electron 在 Windows 上 `transparent: true` 会剥离原生标题栏与可调边框（透明仅在无边框窗口生效），导致窗口失去标题栏且无法拖动、关闭。现 Windows 一律创建带原生边框的不透明窗口（移动 / 关闭 / 调整大小 / Windows 11 系统圆角 / 窗口投影全部恢复）；「窗口效果」在 Windows 下降级为仅窗口透明度生效（毛玻璃材质需透明窗口才能透出，而内容区本就不透明，此前实际不可见），设置页与使用说明文案已同步标注。Linux / macOS 透明与毛玻璃行为不变

## [0.4.5] - 2026-09-20

### 新功能

- **反向链接定位到引用行**：点击反向链接弹层条目，除打开来源笔记外，编辑器还会滚动定位到引用所在行（居中显示）
- **双链同名笔记消歧**：`[[笔记名]]` 匹配到多篇同名笔记时，点击弹出候选列表（标题 + 完整路径）供选择跳转，不再固定打开第一个匹配；唯一匹配时行为不变

## [0.4.4] - 2026-09-18

### 新功能

- **导出合并 PDF**（FR-2.4.10 扩展）：文件夹/整库菜单新增「导出合并 PDF…」，将多篇笔记合并为单个 PDF（pdf-lib 拼接），页面顺序与源文件一致，图片/公式/代码块全保真
- **图形化冲突解决**（FR-2.8.6 扩展）：同步冲突时不再自动中止 rebase 并提示手动处理，而是保留冲突状态并弹出图形化解决界面；支持查看三方对比（本地/远端/共同祖先）、逐文件解决（接受本地版本、接受远端版本、手动编辑）、解决后继续同步或放弃所有更改
- **窗口半透明+毛玻璃效果**（FR-2.9 扩展）：设置 → 通用 → 外观新增窗口效果和透明度控制；支持 Windows 11 Mica/Acrylic 效果、macOS 毛玻璃效果，Linux 依赖桌面合成器；透明度可调（10%-100%），自动模式根据平台选择最佳效果
- **全局搜索**（FR-2.9 扩展）：侧栏新增搜索按钮（Ctrl+F），支持跨笔记库全文搜索；可按标题/内容筛选，搜索范围下拉多选（所有库或指定库）；搜索结果按相关度排序，显示标题、行号、片段、库名与路径，关键词高亮；点击结果直接打开笔记；索引自动构建，支持手动重建
- **双链引用 P3**（FR-2.4.11 扩展）：笔记重命名时自动将库内其他笔记中的 `[[旧名]]` 改写为 `[[新名]]`；编辑卡右下角「反向链接」悬浮入口 + 弹层（有引用时显示胶囊按钮，点击弹出引用当前笔记的来源笔记列表——仅显示笔记标题、同一笔记多次引用合并为一条，点击跳转，随保存 / 外部修改自动刷新；可在设置中关闭）；侧栏「断链引用」入口（有未解析 `[[...]]` 引用时显示数量，点击打开断链列表）
- **侧栏菜单开关**（FR-2.9.2 扩展）：设置 → 通用 → 侧栏菜单可分别显示 / 隐藏「常用 / 收藏 / 标签 / 断链引用 / 回收站」分区（「笔记库」始终显示）；「显示反向链接」开关（设置 → 通用 → 编辑器）控制编辑卡右下角的反向链接入口；快捷入口区与笔记库区之间新增分隔线（上方分区全部隐藏时自动隐去）
- **双链补全优化**：`[[` 补全改为逐级路径补全——输入 `[[` 显示根级文件夹和笔记，选中文件夹后自动展开下一层；提示框样式匹配应用整体风格
- **自动同步变更触发模式**（FR-2.8.14 扩展）：设置 → 通用 → 自动同步新增「变更触发」模式——保存笔记后 5 秒自动同步一次已关联远程仓库的笔记库，连续变更合并为一轮，单轮未完成时新变更在轮次结束后补跑一轮（不丢变更，不并发）；设置从三态开关改为方式选择（关闭 / 定时 / 变更触发），定时模式才显示间隔选择；兼容旧 `autoSyncEnabled` 设置（true 迁移为 interval 模式）

### 优化

- **窗口底部圆角**：内容区底部两角圆角化（10px），与系统标题栏的顶部圆角视觉一致；窗口默认透明，圆角外透出桌面 / 玻璃模糊层；同时关闭透明窗口的系统投影——系统阴影按方形窗口边界绘制，会从圆角的透明缺口透出（浅色壁纸上呈直角残留），关闭后圆角在任意壁纸上都干净，窗口层次感由内容卡片阴影承担
- **窗口最小宽度保护**（PRD 已知局限毕业）：编辑区设置 min-width 480px、预览区 280px，窗口拖窄时不再压缩卡片至不可用，改为横向滚动；非编辑视图页卡片设 min-width 500px

### 修复

- 修复双链索引运行期从不更新的问题（此前仅启动时构建一次：编辑器中输入 / 修改 / 删除 `[[...]]` 引用后，反向链接面板与断链计数始终是启动时的快照）。现由文件监听驱动增量更新——应用内保存、外部编辑、删除、回收站还原即时生效；git 同步 / 自动同步挂起期间丢弃的变更在同步结束后自动全量重建
- 修复同一行多个 `[[...]]` 引用在索引中互相覆盖的问题（索引详情原按行号存取，同行引用只剩最后一个，反向链接面板显示错误摘要）；现按行内出现序号区分
- 修复路径形式双链 `[[目录/笔记名]]` 被断链检测误报的问题（反向索引 / 断链判断统一按叶子名归属，与预览解析语义一致）；跨库同名引用不再错误计入反向链接
- 修复回收站列表在同毫秒内批量删除时顺序不稳定的问题（sort 对相同时间戳无全序；按插入序补全序，修复容量上限 flaky 测试）


## [0.4.3] - 2026-09-15

### 新功能


- **双链补全自动闭合**：编辑器 `[[` 补全列表选中笔记后自动补全闭合 `]]`，光标停在闭合符前（便于继续追加 `|显示名`）
- **导出 PDF**（FR-2.4.9）：侧栏树 / 网格卡片的 ⋮ 菜单「导出 PDF…」，支持单篇与文件夹 / 整库递归批量（跨库可选多次执行）；每篇一个独立 PDF（Chromium printToPDF 管线，公式 / 代码高亮 / 表格 / 内嵌 HTML / 图片全保真，白底黑字打印样式）；图片内联 base64 使 PDF 自包含；批量带进度与失败汇总（单篇失败不中断）；导出目录会话内记忆
- **回收站过期清理**（FR-2.7.6）：设置 → 通用可配置保留天数（7 / 30 / 90 天 / 永久保留，默认 30 天），应用启动时自动清理超期条目（本体 + 索引一并删除）
- **定时自动同步**（FR-2.8.14）：设置 → 通用可开启定时同步（每 1 / 5 / 10 / 30 分钟），对所有已关联远程仓库的笔记库静默执行同步管道；仅同步磁盘已保存内容，失败静默记录日志（状态由库徽标与编辑页自然反映），同步期间挂起文件监听
- **回收站容量上限**（FR-2.7.7）：设置 → 通用可配置最大条目数（100 / 200 / 500 / 1000 / 不限，默认不限）；移入新条目与应用启动时，超出上限即按删除时间从旧到新永久删除（FIFO）
- **导出 HTML**（FR-2.4.12）：侧栏树与网格卡片 ⋮ 菜单「导出 HTML…」，范围与导出 PDF 一致（单篇 / 文件夹递归 / 整库批量）；每篇一个自包含 HTML（居中限宽阅读排版，随系统深浅色，公式 CSS 与字体 data URI 内联，图片 base64），同名自动追加序号
- **主题包导入**：设置 → 通用 → 外观 → 配色支持从 JSON 文件导入自定义主题（浅/深两套 CSS 变量覆盖，19 项变量白名单校验），可删除；自定义主题与内置预设并列显示

### 修复

- 修复导出内容包含 frontmatter 的问题（PDF 与 HTML 导出统一在渲染入口剥离 frontmatter）
- 修复库内容笔记卡片菜单出现重复「导出 PDF…」项的问题（此前批量替换遗留的重复菜单项）
- 修复批量导出 PDF 的进度提示不消失的问题（ElMessage 实例管理错误导致 duration:0 的进度消息无人关闭；改为悬浮进度条组件，完成即消失）
- 修复导出后关闭应用进程不退出的问题（隐藏导出窗口未销毁，阻止 window-all-closed；现随主窗口关闭即销毁）

---

## [0.4.2] - 2026-09-15

### 其他

- 版本号递增发布（安装包基线对齐；0.4.1 之后合入 main 的变更集——双链补全自动闭合、回收站容量上限、导出 HTML、frontmatter 标签等——随 0.4.3 一并发布）

## [0.4.1] - 2026-09-14

### 新功能

- **标签系统**：支持创建/重命名/删除标签（含自定义颜色）；笔记打标签/取消标签（侧栏树 + 网格卡片均有「标签…」入口）；侧栏「标签」区按标签筛选笔记。**标签随笔记 frontmatter 存储**（YAML `tags` 键，Obsidian 兼容）——移动 / 重命名 / 外部编辑天然跟随；重命名 / 删除标签自动改写关联笔记；旧版元数据关联启动时自动迁移。
- **预设主题包**：新增暖色、冷色、高对比度 3 套预设配色，每套含浅色/深色变体；设置页「外观」区新增配色网格卡片选择，持久化到设置。
- **网格/列表切换视图**：常用、收藏、笔记库三个视图新增网格/列表切换按钮，列表模式以紧凑行展示；选择持久化到 localStorage。
- **编辑器 HTML 标签自动闭合**：输入 `<div>` 等开标签的 `>` 时自动生成闭合标签 `</div>`，自闭合标签（`<br>`, `<img>` 等）不触发。

### 优化

- **预览 / 悬浮预览隐藏 frontmatter**：笔记头部的 YAML frontmatter（tags 等）不再渲染为可见内容；采用行级掩码（保留行号映射），行级滚动同步不受影响。网格卡片摘要同步排除 frontmatter
- **网格卡片摘要自适应高度**：摘录不足 3 行时卡片高度自动收缩，不再固定 3 行留白。

### 变更

- **标签颜色修改改为弹窗选色器**：标签 ⋮ 菜单「更改颜色」由静默循环切换下一色改为弹窗——8 色预设色板 + 自定义拾色器，确定后生效。

### 修复

- 修复侧栏树笔记的「标签…」弹窗显示「暂无标签」的问题（弹窗按需挂载时标签加载逻辑未执行，现挂载即加载）
- 修复标签筛选视图为空时误显示笔记库空态文案的问题（现提示「该标签下还没有笔记，可在笔记的『标签…』菜单中添加」）
- 「标签」区块标题不再随选中标签高亮（与「选中笔记不点亮其父文件夹」的行为一致，仅被点击的标签行保留选中态）
- 标签行与标签选择清单禁用文本选择，连点不再出现蓝色选中底色
- `npm run lint` 报错：ESLint 配置忽略 `scripts/` 但 lint 命令仍包含该目录导致报错，从命令中移除。

### 文档/需求

- 补记 FR-2.4.11（`[[` 笔记路径补全）：该能力已随 0.4.0 的双链功能一并实现（排除自引用、支持路径过滤），此前需求条目缺失，本版补记。

## [0.4.0] - 2026-09-13

### 新功能

- **`[[双链]]` 引用**：预览中 `[[笔记名]]` 语法渲染为可点击链接，按笔记名全局匹配（大小写不敏感），断链显示删除线样式。支持 `[[路径|显示名]]` 自定义显示名。
- **页内锚点跳转**：预览中 `[text](#anchor)` 锚点链接可点击跳转，标题自动生成 `id` 属性。编辑器输入 `#` 时自动补全文档内已有锚点、输入 `[[` 时自动补全当前库笔记路径（排除自引用）。
- **文件/文件夹移动**（库内）：侧栏树和网格卡片的 ⋮ 菜单新增「移动到…」，弹出文件夹选择对话框，支持将笔记或文件夹移动到库内任意位置。复用重命名校验管道，含目标重名检查、深度限制（6层）、防止移到自身内部等校验。移动后自动改写笔记内的相对路径引用（图片、链接），引用目标同在移动范围内时跳过。
- **内置 Git**（FR-2.8.13）：应用随安装包内置精简 Git（Windows MinGit busybox / Linux dugite-native），系统未安装 Git 也能完成云端同步。
  - 交付：构建时 `npm run fetch:git` 下载并按 SHA256 校验，解压到 `vendor/git/<平台>/`（不入库）由 electron-builder 收进 `resources/git/`；安装包自身压缩载荷，**运行期零解压代码**。实测 Windows 安装包 125.0 → **144.6 MB（+15.7%）**。
  - 检测：用户触发关联或同步时才检测系统 Git 是否可用，不在启动时打扰；不可用时弹窗引导选择内置 Git 或给出平台安装指引。
  - 偏好：选择持久化到设置（`gitSource`），避免重复弹窗；设置页新增「Git 信息」区块，展示系统/内置 Git 版本并支持重置选择。
  - 二进制不入库：`vendor/` 已加入 `.gitignore`，避免 ~76 MB 二进制进入 git 历史。
- **常用网格卡片显示相对时间**：常用视图卡片底部显示「3 分钟前」「2 天前」等相对时间，替代原 vault/路径信息（路径仍可通过 tooltip 悬浮查看）。
- **笔记「信息」菜单**：笔记的 ⋮ 菜单新增「信息」项，弹窗显示创建时间和最后修改时间（侧栏树 + 网格卡片均支持）。

### 修复

- **内置 Git 路径含空格/中文时无法调用**：simple-git 对 `binary` 做字符白名单校验（不允许空格与非 ASCII），安装在 `C:\Program Files\…` 或中文用户名目录下会抛错。已通过 `unsafe.allowUnsafeCustomBinary` 放行（路径来自 `process.resourcesPath`，非用户输入）。
- **路径形式双链（`[[目录/笔记名]]`）无法跳转**：`resolveByName` 仅比对叶子文件名，带路径的双链永远匹配失败显示删除线；补全却会生成路径形式——现已修复为同时比对完整相对路径。
- **双链补全未正确排除当前笔记**：自引用过滤只取叶子文件名，导致不同目录下的同名笔记也被误排除；改为完整相对路径比较。
- **悬浮预览不跟随编辑器滚动**：悬浮预览是独立的 `MarkdownPreview` 实例，未纳入滚动同步；现已接入，关闭时自动将固定预览同步到当前编辑位置。

### 构建/工程

- `electron-builder.yml`：`extraResources` 改为每平台分别声明——平台段会覆盖顶层同名配置，否则示例插件会随内置 Git 改动一起丢失。
- CI：新增 `vendor/git` 缓存与 `npm run fetch:git` 步骤（在 electron-builder 之前）。
- 新增单测 16 项：`bundledGit` 纯函数 14 项 + 内置 Git 真实同步链路 2 项。

### 设计/规划

- **内置 Git 设计定稿并实施**（`ai/requirements/2026-09-10_bundled-git/bundled-git-design.md`）：D-BG1–D-BG11。关键决策：交付方式选**随包内置**（D-BG2）；打包形态由「运行时解压」**修订为 build 时解压**（D-BG8）——实测安装包增量更小（+19.6 vs +32.9 MB）、且省 33 MB 磁盘与一套解压代码；二进制不入库（D-BG11）。isomorphic-git 因 rebase 支持不完整仍不采用（D-BG9）。

## [0.3.8] - 2026-09-09

### 其他

- 版本号递增发布（无功能变更；由另一开发机执行，使安装包基线与代码同步）

## [0.3.7] - 2026-09-09

### 新功能

- **内嵌 HTML 支持**（FR-2.4.7）：预览渲染笔记中的内嵌 HTML（div / table / details 等）；输出经 DOMPurify 白名单净化并显式禁用表单 / style / base / iframe 等视觉钓鱼与导航劫持向量，CSP 追加 `form-action 'none'` 兜底（笔记经 git 同步传播，投毒笔记不得借预览攻击）
- **链接跳转**（FR-2.4.8）：预览链接统一事件委托——外部 http(s) 经系统浏览器打开；库内笔记相对链接（markdown 与原始 HTML 两种来源）在应用内直接打开；断链渲染为删除线并轻提示；悬浮预览中同样可跳转（跳转即关闭悬浮）
- **行级双向同步滚动**（FR-2.4.1 升级）：块级元素注入源码行号（data-source-line），编辑器与预览双向按行映射对齐（Typora 式），100ms 防回环守卫，图片加载后自动重对齐；顺带修复旧版比例同步实际从未生效的问题（误将组件实例当滚动容器赋值）

- **GFM 任务列表**：支持 `- [x] 第一节内容` / `- [ ] 第二节内容` 语法（有序列表同样支持），渲染为主题色只读复选框，勾选态取自源文件；复选框为只读（笔记以文件为唯一事实来源，编辑源码切换勾选）

### 变更

- markdown-it 渲染管道抽取为 `lib/markdown.ts` 独立模块（单测可直接覆盖）；texmath 类型声明移至 shared（双端可见）

## [0.3.6] - 2026-09-09

### 新功能

- **设置页「返回编辑」**：正在编辑笔记时打开设置，页头显示「正在编辑：×××」与「返回编辑」按钮，一键回到原笔记并自动聚焦继续输入；`Ctrl+,` 变为开关（设置页再按一次返回）；`Esc` 在设置页同样返回（无编辑中的笔记则回欢迎页）
- **定位笔记（FR-2.3.7）**：编辑卡顶栏新增「定位」按钮（十字准星图标），一键在侧栏树中展开并高亮当前笔记；网格卡片 ⋮ 菜单新增「在侧栏中定位」（库 / 文件夹 / 笔记卡片均支持）。侧栏收起时自动恢复；专注模式下以浮层侧栏展示定位结果，不打断沉浸。目标行滚动到可视区并高亮约 2 秒后淡出
- **全局快捷键体系**（修饰键分层：Ctrl=通用动作、Alt=视图切换，速查表见 设置 → 通用 → 快捷键）：`Alt+1/2/3/4` 直达常用 / 收藏 / 回收站 / 笔记库网格；`Ctrl+,` 打开设置；`Alt+F` 专注模式；`Alt+B` / `Alt+V` 切换左侧栏 / 预览区；`Ctrl+N` 新建笔记（目标 = 「当前位置」：最近打开的笔记 / 网格钻入 / 侧栏点击所在文件夹，对话框标题显示目标位置）
- **编辑器格式化快捷键**：`Ctrl+B` 加粗、`Ctrl+I` 斜体、`Ctrl+Shift+X` 删除线（复用工具栏的智能插入：选中包裹 / 占位符选中）
- **设置 → 通用新增「快捷键」速查表**：全局 / 编辑器分组展示，数据源为统一注册表（`config/shortcuts.ts`），调整键位只需同步一处
- **Alt+P 快捷键**：编辑视图中一键呼出 / 收起悬浮预览（与长按预览按钮等效，无需鼠标）

### 变更

- **悬浮预览语义重设计（编辑器）**：从「持续显示、手动关闭」改为「一瞥」——长按呼出方式不变，但**点击编辑区 / 开始输入 / 切换笔记即自动收回**，不再需要点 × 清理；卡片头新增「固定」按钮（磁铁图标），一点转正为常驻分栏预览（解决悬浮预览与分栏预览功能重叠、两种手势易混淆的问题）
- **网格卡片长按宽容**：常用 / 收藏 / 库内容网格的笔记卡片**长按（约 0.4s）等同单击**呼出悬浮预览，与编辑器「长按呼出预览」的手势习惯一致，长按不再「没反应」；网格预览卡片头新增「打开笔记」按钮（进入编辑）
- 悬浮预览卡片头的按钮（打开 / 固定 / 关闭）移除原生 title 提示，改为纯图标（样式与应用整体不一致；固定按钮由 📌 字形改为磁铁线形图标，与关闭按钮同风格）

### 优化

- **编辑器工具栏悬浮提示统一**：格式工具栏与定位按钮的原生 title（白框系统样式）换为与应用一致的 Element Plus 悬浮提示，并显示对应快捷键（如「加粗 (Ctrl+B)」）；封装 TipButton 组件供后续复用
- **笔记库网格导航 P2 收尾**：库卡片视觉强化——双列占位 + 强调色浅底 + 大图标大标题（24px 文件夹图标 / 15px 标题），与文件夹、笔记卡片形成尺寸 / 图标 / 色彩多维区分；窄容器自动回退单列。至此网格导航设计（P1 + P2）全部完成
- 创建 / 重命名对话框打开后输入框自动聚焦（原聚焦时机在动画完成前，时常失效需手动点击）
- **工具栏插入智能化**：加粗 / 斜体 / 删除线——有选中文字时直接包裹选中内容（而非插入示例占位符）；无选中时插入占位符并自动选中，直接打字即替换。行内公式 / 公式块改为只插入定界符（`$ $` / `$$ $$`），不再预填示例公式
- 预览区代码块语法高亮修复：markdown-it 此前未配置 highlight，hljs 已注册的 16 种语言从未参与渲染；现按语言高亮（未识别语言回退转义纯文本），配色随浅 / 深色主题切换（与编辑器观感一致）
- 编辑器标题折叠按钮美化：默认小灰箭头改为与侧栏折叠箭头同风格——弱化常态色、18px 圆角热区、悬停底色反馈，箭头随主题配色；折叠后的「⋯」占位符改为圆角胶囊样式（强调色浅底）

### 修复

- 修复 Windows 上 git 同步后笔记行尾被改为 CRLF 的问题：git 检出时按全局 `autocrlf` 配置转换行尾，破坏应用基于 hash 的防覆盖比对。现在应用管理的库在所有 git 调用中强制 `core.autocrlf=false`，笔记内容逐字节保持一致（同时修复了 Windows 上 3 项 git 集成测试的长期假失败）
- 修复 `Ctrl+N` 新建笔记目标与直觉不符的问题：改为「当前位置上下文」机制——最近一次导航到的地方（打开笔记 / 网格钻入库或文件夹 / 侧栏点击库与文件夹行）即为目标，取代原先「编辑器残留状态优先」的静态优先级（此前从卡片进入 a2 后新建仍落在 a1）；库重命名 / 删除时上下文同步迁移与清理
- 修复「笔记在应用外被修改」横幅误弹的问题：应用自己的自动保存写入也会触发文件监听事件，若此时正在输入（dirty）会被误判为外部修改——横幅弹出约 1 秒后随下一次自动保存消失。现在外部修改检测会先读磁盘与最后写入内容比对，一致即视为自身保存的回声并忽略，横幅只在真正的外部修改时出现
- 修复悬浮预览呼出后无法继续输入的问题：预览按钮点击 / 长按与 Alt+P 都会让键盘焦点滞留在按钮上（按钮外围出现焦点框），CodeMirror 无焦点则既不能打字、「输入自动收回」也随之失效。现在悬浮预览打开 / 关闭后焦点自动交还编辑器；预览切换与专注模式按钮点击后同样归焦编辑器

## [0.3.5] - 2026-09-09

### 新功能

- **Git 网络代理**：设置 → 通用新增「网络代理」区块，可为 Git 同步配置 HTTP/HTTPS 代理（支持 `http://用户名:密码@主机:端口` 格式）；「测试连接」按钮对 github.com 发起轻量请求验证可达性（15 秒超时）；代理仅在 git 调用时注入，不写入 `.git/config`
- **笔记库网格导航（P1）**：库卡片双击钻入库内容，文件夹卡片（图标 + 名称 + 计数）与笔记卡片混排展示，单击笔记悬浮预览、双击编辑；面包屑逐级回退（返回箭头 / 点任意级 / Esc 分级回退）；文件夹 ⋮ 菜单支持在网格内直接新建文件夹与笔记；钻入 / 回退带方向滑动动画

### 其他

- 需求文档新增「Git 网络代理配置」二期规划项（FR-2.8.11 / FR-2.10.4）：为无 GitHub 直连环境的用户提供代理设置

### 优化

- **Git 同步的网络错误体验**：同步操作增加 60 秒超时上限（网络不通时同步按钮不再无限转圈）；常见网络故障（DNS 解析失败 / 无法连接 / 超时 / 连接中断 / 令牌失效等）的错误提示改为友好的中文说明，原始报错仅记录日志

### 修复

- 修复侧栏下拉菜单关闭时左上角闪现菜单残影的问题（第二处回归：v0.3.4 的闪影修复只覆盖了「锚点被删除」路径，本次确认并修复「关闭瞬间 popper 被重定位到视口原点」的路径——下拉弹出/收起不再播放过渡动画，五条关闭路径逐帧截图验证残影为 0）
- 修复侧栏「笔记库」区块的创建按钮与齿轮/加号下拉菜单点击无效的问题（v0.3.4 的悬浮提示统一改动引入回归：el-tooltip 包裹交互元素会拦截点击、破坏下拉事件绑定；已改为 tooltip 只包非交互元素，下拉触发器回退原生 title）
- 修复侧栏下拉菜单关闭时左上角闪现菜单残影的问题（行内按钮组在鼠标离开行时立即隐藏，导致收起中的菜单 popper 失去锚点跳到视口左上角；现菜单打开期间保持按钮组可见、关闭后延迟 300ms 隐藏）

### 变更

- 移除侧栏全部 Element Plus 悬浮提示（与常用/收藏视图风格不一致、信息价值低）；库行描述悬浮保留原生 title；下拉触发按钮以原生 title 提示用途

## [0.3.4] - 2026-09-08

### 新功能

- **侧栏收起 / 呼出与迷你导航条**：侧栏底部新增「收起侧栏」按钮；收起后左侧泊位变为迷你导航条——常用 / 收藏 / 回收站 / 笔记库四个图标直达对应视图（当前视图高亮），底部按钮呼出侧栏；状态跨启动记忆，与专注模式互不干扰。**专注模式下导航条同样可用**，呼出按钮以浮层展示侧栏（不挤压编辑区，点击空白 / Esc / 再点按钮关闭）

- **专注模式隐藏顶栏（设置）**：设置 → 通用新增「专注模式隐藏顶栏」开关；开启后进入专注模式时顶栏（面包屑 / Git 状态 / 视图开关）与格式工具栏整体隐藏，鼠标移到编辑卡顶部热区即整体滑出（悬停期间保持，离开 300ms 后收起；外部修改警告仍常显）

### 修复

- 修复专注模式下呼出的浮层侧栏超出窗口底部的问题（侧栏根元素的 height:100% 与浮层的 top/bottom 约束冲突）

### 优化

- 侧栏「回收站」标题显示条目计数角标（与常用 / 收藏一致）；删除笔记 / 文件夹 / 笔记库后计数即时刷新
- 网格卡片的悬浮提示从浏览器原生 title 换为 Element Plus tooltip（深底白字、圆角投影，与应用整体风格一致；延迟 400ms 避免掠过时频繁弹出）
- 侧栏悬浮提示全部统一为 Element Plus tooltip：区块标题、展开箭头、库行描述（无描述不弹）、笔记 / 文件夹名截断提示、各操作按钮
- 回收站视图头部改为与常用 / 收藏网格一致的标题风格（图标 + 标题 + 计数 + 操作），头部样式提升为网格视图共用

## [0.3.3] - 2026-09-08

### 修复

- CI 修复：tag 触发的版本校验步骤补 `shell: bash`（Windows runner 默认 pwsh 无法解析 bash 语法导致构建失败）；移除 push main 的冗余构建（发布仅由 tag 驱动，手动 workflow_dispatch 保留）

## [0.3.2] - 2026-09-08

### 新功能

- **笔记库描述**：创建笔记库时可填写可选描述（最多 20 字）；鼠标悬浮侧栏库行即显示描述。描述为应用级元数据，存应用数据目录，不写入笔记库（不污染 git 仓库）；库重命名时描述跟随，删除时一并清理
- **笔记库卡片网格**：点击侧栏「笔记库」标题，主区域以卡片网格展示全部笔记库（名称 + 描述 + Git 关联状态），卡片 ⋮ 菜单支持重命名 / 删除笔记库；与常用 / 收藏网格交互一致

### 变更

- 发版一致性保障：CI 在 tag 触发时校验标签与 package.json 版本一致（不一致构建失败）；新增 `postversion` 钩子，`npm version` 后自动 push 标签触发发布；附 vaultMeta 服务单元测试

## [0.3.1] - 2026-09-07

> 注：v0.3.1 标签创建时 package.json 尚为 0.2.0（版本提交在标签之后），该版安装包文件名实际为 `平台-v0.2.0-x64`。版本一致性校验自 v0.3.2 起生效，此后不再发生。

### 新功能

- **常用 / 收藏卡片网格**：点击侧栏「常用」或「收藏」标题，主区域以卡片网格展示笔记（标题 + 内容摘要 + 所属库 / 路径）；**单击**卡片自右侧呼出悬浮预览，**双击**直接进入编辑；再次点击标题或 × 关闭。网格打开时编辑 / 预览卡自动让位，且不影响用户保存的分栏偏好
- **卡片操作菜单**：网格卡片悬浮出现 ⋮ 菜单——常用区提供 收藏 / 移出常用（仅移出列表记录，不删除笔记文件）/ 删除笔记；收藏区提供 取消收藏 / 删除笔记；删除为红色警示，确认后移入回收站
- **取消收藏**：除收藏区卡片菜单外，笔记树菜单按状态显示「收藏笔记 / 取消收藏」（此前收藏后无法取消）

### 修复

- 修复「移出常用」点击后列表不刷新的问题（`recent:remove` IPC 未返回结果对象，渲染端读取时抛错）
- 修复点击「移出常用」后视口左上角闪现菜单残影的问题：下拉菜单的 popper 锚点行在收起动画完成前被列表刷新移除所致。卡片菜单在执行移除类操作前等待收起动画结束；侧栏改为单行入口后此类锚点行已不存在
- 修复进入专注模式后预览卡片不隐藏的问题：进入专注时自动收起预览并记住进入前状态（退出后恢复）；专注模式下用预览按钮呼出的预览与编辑区各占 50%（与需求一致），且持久化的预览偏好不再被专注切换覆盖

### 变更

- **侧栏「常用 / 收藏」改为单行入口**（参考语雀设计）：点击标题直接打开主区域卡片网格，行内列表与展开箭头移除，数量以角标显示；收藏过多时不再拉长侧栏，也从结构上消除了下拉菜单锚点失效导致的闪影
- **回收站改为卡片风格列表**：条目以与常用 / 收藏网格一致的圆角卡片呈现（图标 + 名称 + 类型 + 原位置 + 删除时间 + 操作），保留列表形态，「网格 / 列表切换」为规划功能
- 网格悬浮预览支持**点击空白区域关闭**（此前仅 × / Esc）
- 侧栏区块标题精简：去掉「笔记库」前的文件夹图标与各区块占位块，每个标题仅保留一个前导元素（图标或展开箭头），标题文字列对齐
- 侧栏展开按钮美化（保留于「笔记库」区）：填充三角替换为细线箭头，旋转过渡更平滑，箭头热区带悬停底色反馈
- 右键 / 操作菜单顺序统一：普通操作在上，「删除」（红色警示）永远放最后并以分隔线隔开
- 新增 `recent:remove` IPC 通道（移出常用）

## [0.2.0] - 2026-09-06

### 界面重构

- **三栏圆角卡片布局**：侧栏、编辑区、预览区成为灰色画布上的三张独立圆角卡片，间距统一 10px；欢迎 / 回收站 / 设置页同步采用卡片风格
- 面包屑导航与格式工具栏并入编辑卡片，移除原生菜单栏（撤销 / 重做内嵌工具栏）
- 分栏拖拽间隙优化：分隔条改为透明间隙，悬停显示圆角细条，全部分栏间距统一为 10px

### 新功能

- **预览收缩**：编辑卡顶栏一键隐藏 / 显示预览卡片，状态跨启动记忆
- **专注模式**：四角方框按钮隐藏侧栏只留编辑区；专注下仍可呼出预览（编辑 / 预览各占一半）
- **悬浮预览**：长按预览按钮（约 0.5 秒）呼出悬浮预览卡片，覆盖编辑区不改变分栏布局；`Esc` / × / 再点按钮关闭
- **附件目录可配置**：设置 → 通用中自定义粘贴图片的保存目录，支持多级路径（如 `media/image`）

### 修复

- 修复分栏间隙不一致（分隔条被两侧间距叠加，30px → 统一 10px）
- 修复专注模式下预览按钮无响应的问题
- 修复侧栏笔记高亮在切换到回收站 / 设置页后不消失的问题
- 修复中文系统下新建仓库后关联失败的问题（仓库已创建但应用误报失败）
- 修复侧栏父子层级缩进错位
- 修复中文 locale 下读取未初始化仓库的 git 状态时产生无谓错误日志

### 变更

- 界面用语「子目录」统一改为「文件夹」
- 删除类菜单项（删除笔记库 / 文件夹 / 笔记）改为红色警示
- 回收站入口简化：点击「回收站」标题直达列表，去掉二级「查看回收站」
- 图片引用路径改用内容 hash 做外部修改检测，替代毫秒精度不可靠的时间戳

## [0.1.0] - 2026-09-05

首个公开版本。

### 核心

- 笔记库 → 文件夹（最多 6 层）→ 笔记的树形管理，创建 / 重命名 / 删除全程重名校验与非法字符过滤
- Markdown 编辑（CodeMirror 6）+ 实时预览：LaTeX 公式（KaTeX）、代码高亮、格式工具栏
- 自动保存（1s 防抖）与外部修改保护
- 图片粘贴 / 拖入，存入库内附件目录，相对路径引用
- 收藏与常用（最近打开）列表
- 应用级回收站：还原 / 彻底删除 / 清空

### 云同步

- GitHub Personal Access Token 登录（系统加密存储，含新手引导）
- 笔记库关联已有 / 新建远程仓库，一键同步（提交 → 拉取 → 推送）
- 冲突检测：列出冲突文件并保护本地内容

### 其他

- 浅色 / 深色 / 跟随系统主题
- 插件骨架（manifest + 加载器 + 管理页）与示例插件
- Windows（NSIS）/ macOS（DMG）/ Linux（deb + AppImage）安装包
